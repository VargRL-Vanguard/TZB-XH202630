# 前端整改任务分工表（夺奖专项 · 军令状）

> **发布人**：队长（兼 A 区负责人）
> **发布日期**：2026-08-17
> **适用人员**：A/B/C/D 区负责人 + 前端 F1/F2，全员到岗
> **总目标**：**擂主（12 万）**。评分表中用户体验 15 分 + 完整性 30 分 + 技术创新 25 分共 **70 分直接由前端画面决定**，前端做不出来，后端 148 个测试全是废纸。
> **规范真源**：`backend/prompts/40_前端开发_提示词.md`（必读，开工前逐条过一遍 §17 自查题）；**契约真源**：根目录 `api-doc.js` + Swagger 实测（`http://127.0.0.1:8000/docs`）。本文档与提示词冲突时，以提示词为准。
> **提交死线：2026-09-05。9-01 代码冻结。**

---

## 一、现状定性（为什么发这份文件）

2026-08-17 前端体检结论，全员知悉：

| 检查项 | 结论 |
| --- | --- |
| 后端对接 | 6 个页面 **0 个真实 HTTP 请求**，全部被 `useRealAPI:false` 假开关屏蔽 |
| 登录 / 鉴权 | **不存在**。全项目搜不到一行 token / Authorization 代码 |
| Agent 协同大屏（夺奖核心） | **不存在**。无 WebSocket，5 类事件 0/5 |
| AI 对话 | 关键词子串匹配 + 预制文案 + `setTimeout` 假延迟，评审一问就穿帮 |
| 质量指标看板 | **不存在**（后端 A-05 已产出 `latest.json`，前端没接） |
| XSS 安全 | `ai-chat.js` 用户输入未转义直插 innerHTML，其余页面共 10+ 处同类隐患（红线违规） |
| Mock 纪律 | 未对齐 B 区三画像（p-001/002/003），与 e2e 报告、演示视频口径对不上 |
| 技术栈 | 违规。要求 Vue3+Vite+TS+Element Plus+ECharts，实际是原生 HTML 散落根目录，无路由守卫、无请求封装、无构建 |

**P0 实际进度 ≈ 5%，距 8-29「全 P0 就绪」死线剩 12 天。本表即军令状，从今日起严格执行，无讨价还价空间。**

---

## 二、整改总决策（全队统一，不许再议）

1. **推倒重做**：在项目根新建 `frontend/`，技术栈 **Vue 3 + Vite + TypeScript + Element Plus + ECharts 5（按需引入）**，原生 WebSocket（禁用 socket.io-client），vue-router 带路由守卫，Pinia 全局状态。开工当日在群里定死，**中途换库 = 红牌**。
2. 根目录现有静态页（index/profile/activity/learning-path/suggestions/ai-chat 共 6 套）**降级为视觉参考**，8-18 前由 F2 移入 `legacy_static/`，不参与交付、不参与演示、不参与答辩。
3. **后端四区负责人亲自下场写本区对接页**（自己的接口自己最熟，联调零沟通成本）；前端 F1/F2 承担公共组件、视觉规范、边界态与部署。谁的区域页面在死线拿不出来，责任就在谁。
4. Mock 统一放 `frontend/src/mock/fixtures/`，用 `VITE_USE_MOCK` 开关切换；**请求路径与字段名必须与 `api-doc.js` 一字不差**；mock 数据必须使用 B 区三画像（p-001 本科应届生 / p-002 高职在读生 / p-003 企业转岗人员），质量指标卡数值用真实报告值：**幻觉率 4.10% / 适配准确率 100% / 覆盖率 100%**。
5. 契约冲突处理（1 小时内解决）：群里发「接口路径 + 期望字段 + 实际返回截图」并 @ 对应后端负责人（`/api/auth|user|chat` → A；`/api/student|activity` → B；`/api/learning-path|suggestions` → C；其余 → D）；结论出来前**暂停该接口开发，先做其他页面，禁止自行猜字段**。
6. 9-01 起代码冻结：只修 bug，不加功能、不重构、不换库。

---

## 三、死线倒排（贴墙版）

| 死线 | 节点 | 第一责任 |
| --- | --- | --- |
| **8-18** | `frontend/` 骨架建立、技术栈定死、`legacy_static/` 迁移完成 | 全员 / F2 |
| **8-19** | 基建完成（路由守卫/请求封装/WS store/mock 开关）+ **登录页联调通过**（比原计划提前 1 天） | A |
| **8-21** | 公共组件库 v1（骨架屏/空态/错误态/指标卡/难度徽章） | F1 |
| **8-22** | B 仪表盘、C 资源页、D 大屏骨架（mock 驱动）全部可演示 | B / C / D |
| **8-24** | C 路径页 + 建议页、D AI 对话页 + 质量看板 | C / D |
| **8-25** | 真实 API 全量切换（关 mock 直接联调，页面代码零修改） | B / C / A |
| **8-26** | 全页面 4 态（加载/错误/空/断网）补全 | F2 + 各页负责人 |
| **8-27** | ⭐ **Agent 协同大屏完整**：5 类 WS 事件 + 打字机流式 + 辩论视图 + 时间轴 + 回放器 | D |
| **8-29** | ⭐ **全部 P0 就绪 + DEMO_MODE 离线演示可用**（拔网线全流程可演） | 全员 |
| **8-31** | 备用机部署完成 + 两轮全流程彩排 | F2 |
| **9-01** | 演示视频初剪（D-08）+ **前端代码冻结** | D / 全员 |
| **9-03** | 终剪配合 + 全链路 e2e（3 画像用例走通） | 全员 |
| **9-05** | **提交**（构建产物 + 源码入库） | 队长 |

---

## 四、人员分工总表

| 人员 | 模块 | 交付页面 | 硬死线 |
| --- | --- | --- | --- |
| **队长 A** | 基建 + 登录注册 + 聊天 | Login / Register / Chat | 8-19 / 8-23 |
| **成员 B** | 学情仪表盘 + 学习活动 + mock 画像 | Dashboard / Activity | 8-22 / 8-25 |
| **成员 C** | 资源展示（3 形态）+ 路径 + 建议 | Resources / LearningPath / Suggestions | 8-22 / 8-24 |
| **成员 D** | ⭐协同大屏 + AI 对话 + 质量看板 | AgentScreen / AiChat / QualityBoard | 8-27 / 8-24 |
| **前端 F1** | 公共组件 + 视觉/动画规范落地 | components/* 全套 | 8-21 / 8-25 |
| **前端 F2** | 边界 4 态 + DEMO_MODE + 部署双保险 | 全页面 + nginx 离线包 | 8-26 / 8-29 / 8-31 |

---

## 五、任务明细（按人下发）

### 5.1 队长 A —— 基建 + 登录注册 + 聊天页

**基建（所有页面的地基，8-19 前必须交付，阻塞全队要追责）：**

- [ ] `frontend/` 脚手架：Vite + Vue3 + TS + Element Plus 按需引入 + ECharts 按需引入（`echarts/core`）+ ESLint/Prettier
- [ ] `src/router/`：路由表 + **守卫**（未登录访问业务页 → 强制跳登录；404 页带"返回首页"）
- [ ] `src/api/request.ts`：统一封装，自动带 `Authorization: Bearer <token>`，统一拦截 `{code, message, data}` 解包；401 → 清 token 跳登录；500 → 统一"服务异常，请稍后重试"，**禁止透出后端原文**
- [ ] `src/stores/`：token / user / agentEvents（WS 事件流，按 traceId 分组，回放器直接读 store）
- [ ] WS 基建：`ws://127.0.0.1:8000/ws?token=xxx`，30s 心跳 ping、断线指数退避重连（1s/2s/4s…最大 30s）、重连成功页面顶部黄色横幅自动消失；未知事件 type 容错（console.debug，不许报错）
- [ ] `.env.development`：`VITE_API_BASE=http://127.0.0.1:8000`、`VITE_USE_MOCK` 开关
- [ ] token 存储统一封装（内存 + sessionStorage），禁止出现在 URL / console.log

**登录注册（P0-1，8-19）：**

- [ ] `src/views/Login.vue`：用户名 + 密码，回车提交，按钮防抖防重复，错误内联提示（禁 alert）；品牌感首页画面：项目名 + 副标题"领域知识个性化生成与多智能体协同决策系统" + 科技感渐变背景
- [ ] `src/views/Register.vue`：用户名/密码/姓名/角色下拉（student|teacher|admin）+ 可选 education/major；密码预校验 ≥8 位 + 字母 + 数字，校验失败按钮置灰 + 具体原因
- [ ] 登录成功存 token + userId + role，按角色跳转（student → 仪表盘；teacher → 学生列表）
- [ ] 联调真实接口：`POST /api/auth/login`、`POST /api/auth/register`、`POST /api/auth/logout`（A 区自己家的接口，联调不过没有借口）

**聊天页（P1，8-23）：**

- [ ] `src/views/Chat.vue`：会话列表（`GET /api/chat/list`，未读数）+ 消息流（`GET /api/chat/history` 分页）+ 发送（`POST /api/chat/send`）+ 已读标记（`POST /api/chat/read`）
- [ ] WS 实时收 `message` 事件追加渲染；发送失败消息气泡变红 + 重发按钮，**消息不许凭空消失**

---

### 5.2 成员 B —— 学情仪表盘 + 学习活动 + 三画像 mock

**mock 画像 fixtures（8-19 前交付，C/D 都等着用）：**

- [ ] `src/mock/fixtures/profiles.ts`：p-001（本科应届生/高分/期望难度5/弱项 kp12,kp15/雷达偏满）、p-002（高职在读/低分/期望2/雷达偏低→触发降维解释演示）、p-003（企业转岗/低分/期望2/实操导向→触发 practice_guide），字段照抄 `api-doc.js` §2

**学情画像仪表盘（P0-3，mock 版 8-22，真数据 8-25）：**

- [ ] `src/views/Dashboard.vue` + `src/api/student.ts`（`/api/student/info|metrics|dimensions|behavior|knowledge`）
- [ ] 顶部指标卡：学习时长 / 完成率 / 平均分 + 趋势箭头；数字进视口滚动增长动画（800ms ease-out）
- [ ] ECharts 雷达图：6 能力维度
- [ ] 折线图：学习行为周/月/学期切换，300ms 过渡动画
- [ ] 知识掌握度进度条组：强弱项红绿区分，**弱项必须醒目**（它触发后续 Agent 链路）
- [ ] 画像编辑：表单调 `PUT /api/user/profile`（education/major/theoryTestScore/weakKPs/strongKPs），保存后刷新回显正确

**学习活动页（P2，8-25）：**

- [ ] `src/views/Activity.vue`：课程进度网格 + 每周日历热力图（`/api/activity/stats|courses|recent|calendar`）

---

### 5.3 成员 C —— 资源展示（P0-4）+ 路径 + 建议

**个性化资源展示页（P0-4，mock 版 8-22，真数据 8-26）：**

- [ ] `src/views/Resources.vue` + `src/api/path.ts`，三种形态独立渲染组件：
  - [ ] `customized_resource` → 富文本卡片 + 知识点标签 + **citedChunks 引用切片可点开**（防幻觉证据，必须可见）
  - [ ] `practice_guide` → 分步骤 checklist 可勾选
  - [ ] `tiered_quiz` → 难度 1-5 徽章 + 题目列表 + 答题交互
- [ ] 右上角：资源难度 vs 用户期望难度一致性标识（适配准确率可视化）
- [ ] 底部"太难了/太简单了"反馈按钮 → 触发迭代 → 新版本展示，**版本号 +1 + 新旧版本对比入口**（评审看"动态迭代"落地成 UI 的唯一证据）
- [ ] 每卡片：生成时间 / 命中知识点 chips / 难度徽章 / "由领域专家 Agent 生成"署名

**学习路径页（P1，8-24）：**

- [ ] `src/views/LearningPath.vue`：时间线 completed/current/pending 三态 + 今日任务 checklist（`/api/learning-path/overview|timeline|modules|tasks`）

**学习建议页（P1，8-24）：**

- [ ] `src/views/Suggestions.vue`：分类 tab（method/resource/review/practice）+ 优先级标签（`/api/suggestions/list`）

---

### 5.4 成员 D —— ⭐协同大屏 + AI 对话 + 质量看板（夺奖主菜）

**多 Agent 协同可视化大屏（P0-2，全项目最高优先级，骨架 8-22，完整 8-27）：**

- [ ] `src/views/AgentScreen.vue` + `src/stores/agentEvents.ts`，深色科技风（背景 `#0B1020`，蓝→紫渐变 `#4F6EF7 → #7C5CFC`），值得投入 50% 前端工时
- [ ] 3 张 Agent 卡片横排流水线：学情诊断 Agent（蓝 `#38BDF8`）→ 领域专家 Agent（紫 `#A78BFA`）→ 审核裁判 Agent（金 `#FBBF24`），箭头连线粒子流动画（20-40 粒子）
- [ ] **5 类 WS 事件全量实现，缺一扣一半用户体验分**：
  - [ ] `agent.start` → 卡片转"工作中"（转圈/呼吸灯），进度条跳 step
  - [ ] `agent.thinking` → **打字机流式追加**（30-50ms/字）到思考气泡区
  - [ ] `agent.result` → 卡片"完成 ✅"，data 暂存待 final
  - [ ] `agent.debate` → 辩论对决视图（⚡连线 + topic + 观点气泡左右对撞）
  - [ ] `agent.final` → 关 loading；ok=true 弹资源结果卡，ok=false 错误 + 重试按钮
- [ ] 顶部 traceId + 步骤进度条（1/4 → 4/4）
- [ ] 事件时间轴侧栏（本次 trace 全事件按时间排列，可点击回看）
- [ ] **回放模式**：把 store 里的真实事件序列按原始间隔重放（演示视频补拍 + 现场二次展示的唯一手段）
- [ ] 状态机容错：乱序事件（thinking 早于 start）不许白屏；思考/辩论气泡最多保留 50 条
- [ ] 后端 `/api/visualization/*`、`/api/trace/*` 契约发布后第一时间对齐

**AI 对话页（8-24）：**

- [ ] `src/views/AiChat.vue` + `src/api/aiChat.ts`（`/api/ai-chat/send|history`、DELETE 清空）；AI 回复**必须走后端**，删掉一切前端关键词匹配假回复；AI 生成内容渲染先 sanitize（DOMPurify），**禁 v-html 直插**

**质量指标看板（P0-5，8-24，给评审看的"实力证明"页）：**

- [ ] `src/views/QualityBoard.vue`：3 张大指标卡 —— 幻觉率 **4.10%**（<5% ✅）/ 适配准确率 **100%**（≥85% ✅）/ 覆盖率 **100%**（≥90% ✅）
- [ ] 数据源 `docs/quality_reports/latest.json`（后端 A-05 产出），数值必须与报告一致；达标绿 / 不达标红，**这页是活的不是贴图**
- [ ] 附 3 组画像明细表格（推荐难度 vs 期望、每组幻觉率、覆盖弱项、审核结论）+ 生成时间

---

### 5.5 前端 F1 —— 公共组件库 + 视觉/动画规范落地

**公共组件（8-21 交付 v1，全队等着用）：**

- [ ] `src/components/`：Skeleton（骨架屏，呼吸 1.5s 循环）、EmptyState（插画+引导文案+行动按钮）、ErrorState（插画+一句话原因+重试按钮）、NetBanner（断网黄色横幅）、MetricCard（指标卡，tabular-nums 等宽数字）、DifficultyBadge（难度 1-5 徽章）、AgentCard（大屏 Agent 卡片，状态机 idle/running/done/debating/finished）
- [ ] 所有可交互组件 4 态齐全：default / hover / active / disabled + focus 可见描边；点击反馈（变色/微缩 0.97/波纹），**不允许位移**

**视觉规范落地（8-25 前逐页检查，对照提示词 §4）：**

- [ ] 色彩变量统一：主 `#4F6EF7` / 辅 `#7C5CFC` / 成功 `#22C55E` / 警告 `#F59E0B` / 危险 `#EF4444` / 大屏底 `#0B1020` / 页面底 `#F7F8FC`
- [ ] 字体栈 `"PingFang SC", "Microsoft YaHei", sans-serif`；间距 8px 基数；卡片圆角 8-12px
- [ ] **每个路由中文 `<title>` + favicon**，切换路由 document.title 跟着变（录屏时标签页是画面一部分）
- [ ] 动画参数逐项落地：状态切换 200-300ms ease-out、数字滚动 800ms、页面切换 ≤200ms 淡入、`prefers-reduced-motion` 自动降级
- [ ] 1920×1080 逐页自查：无横向滚动条、无破版、F11 无系统元素入镜、任意时刻截图可当 PPT 素材

---

### 5.6 前端 F2 —— 边界 4 态 + 离线演示 + 部署双保险

**立刻执行（8-18）：**

- [ ] 根目录 6 套旧静态页（index/profile/activity/learning-path/suggestions/ai-chat 及配套 css/js）移入 `legacy_static/`

**边界 4 态全覆盖（8-26，对照提示词 §8 逐页逐条勾）：**

- [ ] 加载态：骨架屏，spinner 不许空转超 3s，超时显示"加载较慢"
- [ ] 错误态：插画 + 一句话原因 + 重试按钮，绝不显示堆栈/code 原文
- [ ] 空态：插画 + 引导文案 + 行动按钮（如"还没有学习记录，去生成你的第一条路径 →"）
- [ ] 断网/后端挂：全局横幅"服务暂不可用"，已缓存数据照常展示（WS 与 HTTP 401 双通道处理）
- [ ] 表单防重复提交、分页末页、token 半路过期，逐页过一遍

**DEMO_MODE 离线演示（8-29，⭐比赛现场保命符，提示词 §13）：**

- [ ] 隐藏快捷键（连按 3 次 `D`）切换 DEMO_MODE：HTTP 全走 `src/mock/fixtures/`（3 画像 + 资源 + 质量报告），WS 事件由内置 trace 回放器按时间轴驱动大屏，登录页预填可一键进入
- [ ] 验收标准：**拔掉网线，从登录 → 仪表盘 → Agent 大屏（含辩论）→ 资源 3 形态 → 质量看板全程可演**

**部署双保险（8-31）：**

- [ ] `npm run build` 产物：sourcemap 关闭、路由懒加载、首屏 JS gzip ≤ 500KB、首屏可交互 < 2s、Lighthouse ≥ 70、控制台无红错
- [ ] 本机 nginx 静态托管 + `start-demo.bat` 一键启动（起 nginx + 开浏览器全屏），零外部服务依赖，**运行时禁止任何外网 CDN 资源**
- [ ] 备用机部署同一套离线包，双方 5 分钟内能开场
- [ ] 演示前一晚 + 当天早上各跑一遍检查单（提示词 §13.3）

---

## 六、协作纪律（每日执行）

1. **每日 18:00 前**群内推送当日进度：完成项 + 页面截图/GIF + 明日计划 + 阻塞项。没截图 = 没做。
2. 阻塞即报：按 §二.5 契约冲突流程 @ 责任人，**1 小时内响应**；禁止静默卡死超半天。
3. Git 分支规范：`feat/fe-<页面>-<简述>`（如 `feat/fe-dashboard-radar`），PR 描述带页面截图（GIF 更佳）；提交前 `npm run lint` 零 error。
4. 组件单文件 ≤ 300 行，复用 ≥ 2 次的 UI 抽公共组件；CSS 用 BEM 或 CSS Modules，禁止全局裸类名互相污染。
5. 每周日 21:00 全员线上对表（30 分钟）：死线达成度 + 风险 + 下周排期。

---

## 七、验收红线（一票否决，9-01 前逐条销项）

1. ❌ 5 类 WS 事件缺一类、thinking 不做流式 → 用户体验分砍半，**不接受**
2. ❌ 界面出现英文报错 / lorem ipsum / console.log 残留 / test 文案
3. ❌ AI 生成内容未 sanitize 直插（XSS）→ 评审现场翻车
4. ❌ mock 字段与 `api-doc.js` 不一致 → 后端上线当天全炸
5. ❌ 数据页面只有成功态，无骨架/错误/空态 → 半成品观感
6. ❌ 1920×1080 破版、横向滚动条、F11 入镜系统元素
7. ❌ 没有 DEMO_MODE 离线演示就上赛场 → 现场任何一环挂掉全盘皆输
8. ❌ token 进 URL / localStorage 硬编码拼接 / 运行时依赖外网 CDN
9. ❌ 9-01 后加新功能、换 UI 库、重构布局
10. ❌ 私改接口字段名 / 路径 / 请求方式

---

## 八、检查与问责（军令状条款）

| 情形 | 处理 |
| --- | --- |
| 死线延误 ≤ 24h，或当日 18:00 无进度推送 | **黄牌** ×1，群内通报 |
| 死线延误 > 24h，或累计 2 张黄牌 | **红牌**：任务立即回收，由队长指派后端接管；答辩贡献名单与获奖排名相应调整 |
| 阻塞超 4 小时未上报导致延误 | 责任自负，按红线论 |
| 按 §三死线提前完成且验收一次通过 | 群内表扬 + 答辩主讲的加分项优先权 |

**验收签字制**：每个 P0 页面完成后，由**队长 + 对应后端负责人双人验收**（功能按本表勾选项 + 红线 §七逐条过），签字后才算交付。验收记录附在 PR 描述。

---

## 附：签署（8-18 20:00 前群内回执"已读并认领"）

| 角色 | 认领模块 | 签名 | 日期 |
| --- | --- | --- | --- |
| 队长 A | 基建 + 登录注册 + 聊天 | | |
| 成员 B | 仪表盘 + 活动 + 三画像 mock | | |
| 成员 C | 资源 3 形态 + 路径 + 建议 | | |
| 成员 D | ⭐大屏 + AI 对话 + 质量看板 | | |
| 前端 F1 | 公共组件 + 视觉/动画规范 | | |
| 前端 F2 | 4 态 + DEMO_MODE + 部署 | | |

> **最后一句给全员**：后端四个区已经把 148+ 测试、三项硬指标（幻觉率 4.10% / 适配 100% / 覆盖 100%）做出来了，实力摆在这。现在差的只是把这套实力**让评审亲眼看见**。8-29 见。
