# 06 前端 F2：边界 4 态 + 离线演示 + 部署双保险

> 负责人：前端 F2　|　死线：**旧页迁移 8-18**、**4 态补全 8-26**、**DEMO_MODE 8-29**、**部署+备用机 8-31**
> 你管的是比赛现场的保命环节：现场 WiFi / MySQL / AI API 任何一个挂掉，全队靠你的离线包活着。

## 复制即用 · 开工提示词

把下面整段复制给你的 AI：

```text
你是本项目的前端工程师，负责边界 4 态全覆盖、离线演示模式（DEMO_MODE）与部署双保险。项目：挑战杯 XH-202630，前端 frontend/（Vue3+Vite+TS），基建由 A 提供，mock fixtures 由 B 提供。

请先按顺序完整阅读以下文件（项目根 d:\TZB\TZB-XH202630）：
1. frontend/prompts/00_开工前必读.md（技术栈与纪律）
2. backend/prompts/40_前端开发_提示词.md（最高规范，重点 §8 边界四态、§9 性能安全、§13 演示日应急预案逐条、§15 验收）
3. frontend/prompts/06_F2_边界4态_离线演示_部署.md（本文档，你的任务清单）
4. frontend/prompts/08_接口契约速查表_字段级.md（⭐字段级契约+DoD：4 态触发条件照 §0 错误处理矩阵实现（401/403/404/500/断网各自唯一行为），DEMO_MODE mock 数据字段照 §1-§6 造，验收按 §8 全局 DoD）

读完后先回答 00 文档 §7 的 6 道自查题，确认无误后按本文档顺序开工。

全程遵守：
- DEMO_MODE 与 VITE_USE_MOCK 是两个开关：前者是演示现场整体离线模式（HTTP 全走 fixtures + WS 走回放器 + 登录预填），后者是开发期单接口 mock，不要混用
- 构建产物 sourcemap 关闭、路由懒加载、首屏 JS gzip ≤ 500KB、运行时禁止任何外网 CDN 资源
- 完成每个任务节点后，主动提醒我完成本文档末尾「手操清单」对应事项（尤其拔网线实测、双机部署），给出具体步骤后再继续
```

## 任务清单

### 阶段〇：旧页迁移（8-18，当天完成）

- [ ] 根目录 6 套旧静态页（index / profile / activity / learning-path / suggestions / ai-chat 及配套 css/js、theme.js 保留引用关系）用 `git mv` 移入 `legacy_static/`，保留 git 历史
- [ ] 确认移完后项目根干净（README/LICENSE/pyproject 等不动）

### 阶段一：边界 4 态全覆盖（8-26）

逐页 × 4 态矩阵过一遍（页面：Login/Register/Dashboard/Resources/LearningPath/Suggestions/Chat/AiChat/AgentScreen/QualityBoard/Activity）：

- [ ] 加载态：骨架屏（用 F1 的 Skeleton）；spinner 不空转超 3s，超时显示"加载较慢"
- [ ] 错误态：ErrorState + 重试；500 统一文案，不透后端原文
- [ ] 空态：EmptyState + 引导行动按钮
- [ ] 断网/后端挂：全局 NetBanner"服务暂不可用"；已缓存数据照常展示；HTTP 401 与 WS error **双通道**都处理
- [ ] 聊天发送失败气泡变红 + 重发；表单防重复（loading + 禁用）；分页末页；token 半路过期
- [ ] 产出《4 态检查矩阵表》贴群：每页 × 每态 ✅/❌，验收当天用

### 阶段二：DEMO_MODE 离线演示（8-29，⭐保命符）

- [ ] 隐藏快捷键：连按 3 次 `D` 切换 DEMO_MODE（页面角落出现小标识"演示模式"）
- [ ] 开启后：HTTP 全走 `src/mock/fixtures/`（3 画像 + 3 形态资源 + 质量报告）；WS 事件由**内置 trace 回放器**按时间轴驱动大屏（用 D 存的真实事件序列 JSON）；登录页预填 student001 一键进入
- [ ] **验收标准：拔掉网线，从登录 → 仪表盘 → Agent 大屏（含辩论）→ 资源 3 形态 → 质量看板，全程可演**

### 阶段三：部署双保险（8-31）

- [ ] `npm run build` 检查：sourcemap false、路由懒加载、首屏 JS gzip ≤ 500KB、首屏可交互 < 2s、Lighthouse ≥ 70、控制台无红错
- [ ] 本机 nginx 静态托管构建产物 + `start-demo.bat` 一键启动（起 nginx + 开浏览器全屏）；零外部服务依赖
- [ ] 备用机部署同一套离线包，双方 5 分钟内能开场
- [ ] 演示前一晚 + 当天早上各跑一遍检查单：离线全流程 / 在线全流程 / F11 无系统元素 / 缩放 100% 分辨率 1920×1080 / 关系统通知 / 备用视频就绪

## 验收标准

- 《4 态检查矩阵表》全 ✅
- 拔网线实测通过（队长在场见证）
- 两台笔记本各演示一遍完整流程

## ⚠️ 手操清单（AI 会提醒你，但必须你亲手做）

| 时机 | 手操内容 |
| --- | --- |
| 8-18 | `git mv` 迁移旧页 + push，群发截图确认根目录干净 |
| 4 态检查 | **亲手**把后端停掉 / 断网开关掰来掰去，逐页截图 4 态发群（AI 模拟不了真断网） |
| DEMO_MODE | **拔网线**（或关 WiFi）完整走一遍演示流程录视频发群 |
| 部署 | 本机 nginx + bat 双击实测；备用机现场装一遍并录 5 分钟开场演示 |
| 演示前 | 按检查单逐项打勾拍照发群（前一晚 + 当天早上） |
| 每个阶段完成 | git 分支 `feat/fe-demo-mode` 等 → commit → push → PR |
| 每日 18:00 | 群推进度：完成项 + 截图/视频 + 明日计划 + 阻塞 |
