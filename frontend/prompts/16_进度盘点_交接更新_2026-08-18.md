# 16 进度盘点 · 交接更新（2026-08-18 深夜实测）

> **本文档是对 `14_交接手册_搭积木总览.md` 的增量更新**：14 号写于 8-17，此后 F1 已交付 AppShell 导航壳 + 品牌占位页 + 组件库 v1，手册中"还缺的积木"一节已过时。
> 给搭积木的人：先读 14 号手册建立全局认知，再用本文档校正现状。5 分钟看完。

---

## 一、自 14 号手册以来新增的积木（全部已实测）

### 1. AppShell 全局导航壳（15 号 T1）✅

登录后不再是"无路可走"，所有业务页自动套壳，**搭积木时无需任何配置**。

| 文件 | 说明 |
| --- | --- |
| `src/components/AppShell.vue` | 布局壳：SideNav 220px + TopBar 56px + 内容区；≤1200px 自动折叠 64px |
| `src/components/SideNav.vue` | 三分组 10 菜单；student 隐藏学生列表 / teacher 隐藏协同大屏 / admin 全显 |
| `src/components/TopBar.vue` | 当前页标题（读 `route.meta.title`）+ 用户胶囊 + 登出（Confirm 二次确认） |
| `src/App.vue` | 按 `meta.layout:'full'`（大屏）与 `meta.public`（登录/注册/404）切换有无壳 |
| `src/router/index.ts` | 仅给 `/agent-screen` 加了 `meta.layout:'full'`，**路由路径零改动** |

新页面接入方式：照旧在 `router/index.ts` 注册路由（带 `meta.title` 中文标题）即可自动获得侧栏 + 标题 + 面包屑级体验；菜单列表在 `SideNav.vue` 的 `menuGroups` 里维护。

### 2. 品牌化占位页（15 号 T2）✅

9 个未开发页面已从"裸陋 h1+p"换成品牌占位：

- 组件：`src/components/ConstructionPage.vue`（props: title/owner/eta/fallbackLink/dark）
- `Dashboard.vue` 特殊版：欢迎横幅 + 骨架屏示意 + 常用功能导航卡（B 组开工后**整体替换该文件**即可）
- `AgentScreen.vue` 深色全屏版（D 组开工后整体替换）
- 其余 7 页（Resources/LearningPath/Suggestions/Activity/AiChat/Quality/Students）均为 10 行薄壳，**直接覆盖文件内容**，无需动路由

### 3. 组件库 v1（15 号 T4 / 05 号规格）✅ 全部交付

| 组件 | 文件 | 关键 props（对齐 08 契约） |
| --- | --- | --- |
| Skeleton 骨架屏 | `components/Skeleton.vue` | `rows` / `variant:'card'\|'text'\|'chart'`，呼吸 1.5s |
| EmptyState 空态 | `components/EmptyState.vue` | `icon` / `text` / `actionText` + `@action` |
| ErrorState 错误态 | `components/ErrorState.vue` | `text` / `retryText` + `@retry`（设计上无法塞堆栈） |
| MetricCard 指标卡 | `components/MetricCard.vue` | `label/value/unit/trend('up'\|'down'\|null)/trendValue/decimals`，进视口 800ms 滚动，tabular-nums |
| DifficultyBadge 难度徽章 | `components/DifficultyBadge.vue` | `level(1-5)` / `expected`（一致 ✅ / 偏高 ↑ / 偏低 ↓），绿→黄色阶全走 CSS 变量 |
| AgentCard 大屏卡 | `components/AgentCard.vue` | `name/themeColor/icon/state(AgentCardState)`，running 呼吸灯、debating ⚡ 金边、默认插槽=思考气泡区 |
| ConstructionPage | `components/ConstructionPage.vue` | 见上，组件库第七个 |
| NetBanner 断网横幅 | `components/NetBanner.vue` | 基建原有，App.vue 全局挂载 |

- **用法文档：`src/components/README.md`**（每个组件一段可复制示例 + props 表）
- **逐态演示页：`/#/demo`**（内部路由不进菜单；悬停/点击/Tab 可验证 4 态）
- 全部自动按需引入，模板直接写标签；`vue-tsc` + `vite build` + `eslint` 全绿

### 4. 首屏加载态（15 号 T3）✅

`frontend/index.html`：内联 CSS 品牌色旋转圆环 +「系统加载中…」+ `theme-color #4F6EF7`，Vue 挂载自动替换，强刷不白屏。

---

## 二、当前未完成事项统计（交接重点 ⬜）

### A. 前端页面（12 个占位壳 → 真页面，最大头）

| 优先级 | 页面 | 负责参照 | 主要积木 | 死线参考 |
| --- | --- | --- | --- | --- |
| P0-3 | Dashboard 学情仪表盘 | `02_B` | MetricCard + ECharts 雷达/折线 + Skeleton | 8-24 |
| P0-2 ⭐ | AgentScreen 协同大屏 | `04_D` | AgentCard + agentEvents store + wsClient | 8-24 |
| P0-4 | Resources 资源 3 形态 | `03_C` | DifficultyBadge + sample_resources | 8-23 |
| P0-5 | Quality 质量看板 | `04_D` | latest.json（数值禁写死） | 8-24 |
| P1 | LearningPath / Suggestions | `03_C` | EmptyState + 契约 §3 | 8-25 |
| P1 | AiChat | `04_D` | DOMPurify（已装） | 8-24 |
| P2 | Activity / Students | `02_B` | 契约 §2b | 8-25 |

> 每页 DoD 逐条在 `08_接口契约速查表_字段级.md` §8；开工 = 复制对应角色文档的「开工提示词」。

### B. 缺失的配套件

| 事项 | 现状 | 参照 |
| --- | --- | --- |
| ⬜ 三画像 mock fixtures | `src/mock/fixtures/` **目录不存在**，B 未交付 | 00 号 §5：必须从 `backend/b_学情数据/test_profiles/*.json` 转换 |
| ⬜ 旧静态页迁移 `legacy_static/` | **未迁移**（旧 html/css/js 仍在项目根） | `06_F2` 阶段〇，原死线 8-18 已超 |
| ⬜ DEMO_MODE 离线演示 | 未开工（演示日保命符，永不砍） | `06_F2` 阶段二，8-29 |
| ⬜ Chat.vue 拆分 | 886 行超 300 行约定 | 15 号明确 8-25 后再动 |
| ⬜ 视觉逐页落地 v2 | 组件已达标；逐页走查未做（中文 title/favicon/4 态/tabular-nums） | 05 号 v2 清单，8-25 |
| ⬜ B/C/D 后端任务清单勾选 | 代码已完成（148 测试全绿），清单 0 勾选，**需补勾** | 各区任务清单 md |

### C. F1 遗留手操（AI 已尽，必须人做，当前全部未做 🔴）

1. **git 提交**：26 个文件改动全部未提交（14 modified + 12 untracked），远程最后提交停在旧静态页阶段
   - 建议分支：`feat/fe-appshell`（壳+首屏）→ `feat/fe-placeholder`（占位页）→ `feat/fe-components`（组件库）
2. 三角色登录截图（student001/teacher001/admin001 菜单差异）
3. 路由走查（10 菜单 + F5 高亮）+ 折叠测试 + 登出 WS 关闭验证 + 1920/1366 双分辨率
4. 组件库逐态截图（`/#/demo` 打开，default/hover/active/focus + Tab 走查）打包发群
5. 群发「组件库 v1 就绪」通知 A/B/C/D 取用

---

## 三、给搭积木人员的接入速查

1. **导航**：新页面只要注册路由带 `meta.title`，壳自动生效；不要往 `App.vue` 里塞布局
2. **组件**：`src/components/README.md` 抄示例即用；先开 `/#/demo` 看实物
3. **占位页替换**：直接覆盖 `src/views/*.vue` 文件内容，路由和菜单都不用动
4. **数据**：`import { get, post } from '@/api/request'`；字段查 08 号契约；mock 等 B 交付 fixtures 前，页面先跑真接口（后端四区已就绪）
5. **大屏页**：记得保留 `meta.layout: 'full'`（全屏无壳），AgentCard 状态直接读 `useAgentEventsStore()`
6. **验证**：`npm run lint`（零 error）+ `npm run build`（vue-tsc 过）→ 08 号 §8 DoD → 截图发群

---

## 四、每日启停（复制即用）

```powershell
# 后端（项目根，窗口别关）
python -m backend.main        # → http://127.0.0.1:8000/docs
# 前端（另开终端）
cd frontend; npm run dev      # → http://127.0.0.1:5173
# 账号：student001 / teacher001 / admin001，密码 Test@1234
```

> ⚠️ 本机注意：项目根无 venv，用系统 Python（`py`）；依赖未装先 `py -m pip install -r backend\requirements.txt`；node_modules 已装。

---

## 五、一句话总结

**14 号手册的"积木清单"再更新：F1 组件库 v1 ✅、导航壳 ✅、品牌占位 ✅、首屏 ✅ —— 剩下就是 12 个页面 + B 的 fixtures + F2 的 DEMO_MODE + 一堆没提交的 git。先提交代码，再开搭 Dashboard。**
