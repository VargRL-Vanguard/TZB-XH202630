# 17 占位页转真页面 · P0 优先级开发计划（2026-08-19）

> 负责人：前端优化（本文档作者）→ 完成后交接给搭积木人员。
> 依据：`08_接口契约速查表` §8 DoD + `02/03/04 号`页面需求 + **后端源码实测契约**（实测与 08 号文档有差异处，以本文档 §三 为准）。
>
> **✅ 2026-08-19 全部完成**：P0→P1→P2 九个页面全部转真 + 后端阻塞修复 + B/C 种子数据 + lint/build 全绿 + teacher 角色接口联调验证通过。可交接。

---

## 一、开发顺序（P0 → P1 → P2）

| 批次 | 页面 | 优先级 | 死线 | 核心积木 | 数据源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 0 | 后端联调修复（见 §二） | P0 前置 | 8-19 | — | — | ✅ 完成 |
| 1 | 基建：api 层 + fixtures + echarts 按需 | P0 前置 | 8-19 | — | — | ✅ 完成 |
| 2 | **AgentScreen 协同大屏** | P0-2 ⭐ | 8-24 | AgentCard + agentEvents + wsClient | WS 5 类事件 + mock trace 回放 | ✅ 完成 |
| 3 | **Dashboard 学情仪表盘** | P0-3 | 8-24 | MetricCard + Skeleton | `/api/student/*` 真接口 | ✅ 完成 |
| 4 | **Resources 资源 3 形态** | P0-4 | 8-23 | DifficultyBadge | fixtures（sample_resources）+ 反馈迭代 | ✅ 完成 |
| 5 | **Quality 质量看板** | P0-5 | 8-24 | MetricCard | `GET /api/quality/latest`（新） | ✅ 完成 |
| 6 | LearningPath / Suggestions / AiChat | P1 | 8-25 | EmptyState + DOMPurify | C 区真接口 + D 区新接口 | ✅ 完成 |
| 7 | Activity / Students | P2 | 8-25 | 契约 §2b | B 区真接口 + fixtures | ✅ 完成 |

## 二、已发现并需修复的阻塞问题（后端）—— ✅ 全部修复

| # | 问题 | 位置 | 修复 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | `backend/main.py` 只挂了 A 区路由，B/C/D 全 404 | `backend/main.py` | include B 区 `routes.py`、C 区 `routes.py`、D 区 api 路由 + 各区 init_db | ✅ |
| 2 | D 区 import 错模块：`backend.公共.auth` 不存在 | `d_AI集成/api/trace.py`、`visualization.py` | 改为 `backend.公共.auth_middleware` | ✅ |
| 3 | D 区缺 `/api/ai-chat/*` HTTP 路由（契约 §4），只有内部函数 | `d_AI集成/chat/*.py` | 新建 `d_AI集成/api/ai_chat.py` 包装 send/history/clear | ✅ |
| 4 | Quality 数据源 `/docs/quality_reports/latest.json` 不存在（无 latest.json、无 HTTP 接口） | `docs/quality_reports/` | 新建 `GET /api/quality/latest` 读最新报告 JSON | ✅ |
| 5 | D 区 AI 无 key 时 `BaseAIProvider.generate` 直接抛异常 | `providers/base.py` | ai-chat 路由捕获 → 返回 `degraded` 标记（前端显示明确降级态，不穿帮） | ✅ |

附加修复（实施中发现）：

- C 区 `learning_path/service.py` 异常分支引用未定义 `logger` → 补 `logger = log`（NameError 修复）
- B/C 区缺演示数据 → 新增 `backend/b_学情数据/seed_data.py`（u001 张三 / u002 李四 + 活动记录）、`backend/c_学习内容/seed_data.py`（学习路径 + 建议），幂等可重复执行

## 三、契约差异备忘（实测后端 vs 08 号文档，前端按实测写）

| 接口 | 08 号文档 | 后端实测（以此为准） |
| --- | --- | --- |
| `/api/student/dimensions` | `[{name,value}]` | `{comprehension,application,analysis,evaluation,creation,collaboration}`（0-100 dict） |
| `/api/student/knowledge` | `[{name,progress}]` | `[{kp_id,kp_name,mastery(0-100),status(mastered/learning/not-started)}]` |
| `/api/student/behavior` | `{labels,values}` | `{period,startDate,endDate,totalStudyMinutes,activityCount,completedCount,avgScore,activityTypeBreakdown,dailySeries:[{date,minutes,count}],_isMock}` |
| `/api/activity/stats` | `totalHours/completedCourses/...` | `{totalActivities,completedActivities,completionRate,avgProgress,totalStudyMinutes,avgScore}` |
| `/api/activity/courses` | `[{id,title,statusLabel,...}]` | `{total,items:[{activityId,activityType,resourceId,resourceName,status,progress,score,startTime,durationMinutes,kpTags,...}]}`（filter=all/in-progress/completed/not-started） |
| `/api/activity/recent` | `[{id,title,desc,time}]` | `{count,items:[Activity.to_dict() 同上]}`（days/limit 参数） |
| `/api/activity/calendar` | `[{day,date,hours,today}]` | `{days,items:[{date,count,minutes}]}`（今天=最后一项） |
| Resources 3 形态 | — | 后端无列表接口；结构以 `backend/c_学习内容/sample_resources/*.json` 为准（fixtures 化），真接口等 C 组 8-26 |
| Students 学生列表 | — | 后端无列表接口；实测方案：seed 学生清单（u001/u002）+ 逐个单查 `/api/student/info`（teacher001 实测 200）；3 画像档案走 fixtures 展示 |
| `/api/ai-chat/send` | `{studentId,message,context}` | 本次新增路由对齐 08 号契约字段 |

## 四、新增文件清单

```
frontend/src/
├── api/
│   ├── student.ts        # info/metrics/dimensions/behavior/knowledge
│   ├── activity.ts       # stats/courses/recent/calendar
│   ├── path.ts           # learning-path 4 接口
│   ├── suggestions.ts    # list/read
│   ├── aiChat.ts         # send/history/clear
│   └── quality.ts        # latest
├── mock/fixtures/
│   ├── profiles.ts       # 3 画像（源自 test_profiles/*.json，禁止手编数值）
│   ├── trace.ts          # 完整 mock trace（start→thinking×N→result→debate→final）
│   └── resources.ts      # 3 形态资源（源自 sample_resources/*.json）
├── utils/echarts.ts      # ECharts 按需注册（雷达/折线）
├── components/agent/     # 大屏子组件：TypewriterBubble / ParticleFlow / EventTimeline / FinalCard
├── components/resource/  # CustomizedResource / PracticeGuide / TieredQuiz
└── views/ 9 个页面全部重写
```

## 五、每页 DoD 对齐（照 08 号 §8，验收时逐条打勾）

- **AgentScreen**：5 类事件真实触发（start 呼吸灯/thinking 打字机/result ✅/debate ⚡/final 弹卡）；进度条 1/4→4/4；traceId 显示；时间轴可点；回放完整重演；杀后端黄色横幅自动恢复
- **Dashboard**：指标卡 800ms 滚动 + tabular-nums；弱项红色醒目；画像编辑保存刷新回显；周/月/学期 300ms 切换
- **Resources**：citedChunks 可点开；指南可勾选；测验可作答；「太难了」→ 版本 +1 + 新旧对比
- **Quality**：3 卡数值与 latest 报告逐项一致（禁止写死）；明细表 3 画像 + 生成时间
- **全局**：4 态齐全；lint 零 error；vue-tsc + build 全绿；1920×1080 无横向滚动

## 六、交接说明（给搭积木人员）

1. 本次交付后，9 个页面全部为真页面；后端 `python -m backend.main` 一次启动四区（A+B+C+D 全挂载）。
2. DEMO_MODE / 真数据切换（8-25/8-26）属于下一阶段：Resources 等 C 组接口就绪后把 fixtures 换成 `src/api/path.ts` 真调用即可（页面已按数据结构分层，替换成本约半天）。
3. git 提交、三角色截图、录屏等手操项仍需人工完成（见 16 号文档 §四）。

### 验收自测路径（2026-08-19 已跑通）

- 后端：`py -m backend.main`（8000 端口，health 返回 db:up）；种子数据已灌（A 区账号 + B/C 区演示数据，重跑 seed 脚本幂等）
- 前端：`npm run dev`（5173 端口）；`vue-tsc + vite build` 全绿、改动文件 eslint 零 error
- 测试账号：`student001 / Test@1234`（学生，首页仪表盘）；`teacher001 / Test@1234`（教师，首页学生列表，实测 `/api/student/info?studentId=u001|u002` 均 200）
- 教师视角串联：学生列表卡片 →「学情详情 / 学习路径 / 学习建议」均带 `?studentId=` 直达对应学生页面
