# 1_用户与聊天/ — 成员 A：用户与基础聊天

> 4 人中的**地基**。A 需先完成本区，B / C / D 才能基于 `userId / token` 开工。

## 负责接口

- 用户认证：`/api/auth/login`、`/api/auth/register`、`/api/auth/logout`
- 用户信息：`/api/user/info`
- 聊天：`/api/chat/history`、`/api/chat/send`、`/api/chat/list`、`/api/chat/read`
- 实时通道：`/ws`（心跳、消息推送、订阅通知）

> 原 `api-doc.js` 中 `/api/chat` 的用户信息接口归本区。

## 建议文件结构

```
1_用户与聊天/
├── 概览.md
├── db.py              # 本区自有 DB 连接（不放 公共/）
├── config.py          # 本区自有配置（USER_CHAT_DB_URL 等）
├── routes.py          # 路由注册入口（仅注册本区接口）
├── auth/
│   ├── register.py
│   ├── login.py
│   └── logout.py
├── user/
│   ├── info.py
│   └── __init__.py    # 暴露 get_user_by_id 供 S-02 调用
├── chat/
│   ├── send.py
│   ├── history.py
│   ├── list.py
│   └── read.py
├── ws/
│   ├── server.py
│   └── handlers.py
├── models/            # ORM 按表分文件
│   ├── user.py
│   └── message.py
└── tests/
    ├── test_auth.py
    └── test_chat.py
```

> A 的任务清单已统一放到 backend 根目录的 `任务清单_1_用户与聊天.md`（含 S-01/S-02 公共任务 + A-00~A-04），本目录不再保留任务清单。

## 依赖 / 被依赖

- **被依赖**：B / C / D 都要用 `userId` 与 `token` → 必须先提供：
  - A-01 完成后，`user/__init__.py` 暴露 `get_user_by_id()` 给 S-02
  - S-02 完成后，所有 B / C / D 路由可加 `@require_auth`
- **依赖**：`公共/` 中的 response / logger / errors / config（基础工具）

## 数据库归属

本区拥有独立的数据库（`USER_CHAT_DB_URL` 指向），B / C / D 任何代码**不可**直连本区数据库，只能调用 `get_user_by_id` 等暴露函数。

## 提交前自查

- [ ] 只动了 `公共/`（如有）和 `1_用户与聊天/` 下的文件
- [ ] 没有触碰 B / C / D 的目录（`2_学情数据/`、`3_学习内容/`、`4_AI集成/`）
- [ ] `get_user_by_id` 接口签名已在群内同步给 A 自己（用于 S-02）
