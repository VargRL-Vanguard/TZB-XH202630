# 任务清单 — 成员 2：2_学情数据/

> **比赛目标**：XH-202630 挑战杯「领域知识个性化生成与多智能体协同决策系统研究」，**奔着夺奖（擂主 12 万）去**。
> **你（成员 2/B）负责**：`2_学情数据/`，是**学情诊断 Agent** + **垂直领域知识库切片** + **≥3 组差异化测试画像**的唯一负责区。
> **目录约定**：`backend/概览.md` ｜ **协议**：`backend/协作协议.md` ｜ **总览**：`backend/任务总看板.md`
> **3 项硬指标**（你直接负责 1 个，间接贡献 2 个）：
> - 幻觉率 `< 5%`　|　画像-难度适配准确率 `≥ 85%`　|　核心知识点覆盖率 `≥ 90%`
> - 你的知识库切片质量直接决定「幻觉率」和「覆盖率」两项是否达标
> **倒推时间线**：报名 2026-05-30～06-30 → 提交 **2026-09-05**（死线）→ 初审 2026-09-20 → 决赛 2026-11
> **每周硬节点**：每周五晚 22:00 在群里同步进度；距提交剩 2 周（8-22）起**每日**同步。

---

# 任务 1：2_学情数据/ 自有数据层

**ID**：B-00
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：无
**优先级**：P0
**归属目录**：`backend/2_学情数据/`

## 涉及文件
- `backend/2_学情数据/db.py`（新建）— 连接池 + `get_session()` 上下文管理器
- `backend/2_学情数据/config.py`（新建）— 读取 `STUDENT_DATA_DB_URL` / `KB_VECTOR_DB_URL`（如使用向量库）
- `backend/2_学情数据/models/`（按表分文件）
  - `student.py` — Student 表
  - `activity.py` — Activity 表
  - `kb_chunk.py`（**挑战杯新增**）— 知识库切片表（chunk_id / doc_id / content / embedding / kp_tags JSON / source_url / version）
  - `test_profile.py`（**挑战杯新增**）— 测试画像集（profile_id / payload JSON / label）

## 描述
学情数据**自有**的数据层，**不**放在 `公共/`。本任务要一并把挑战杯需要的**知识库切片表**和**测试画像表**的 schema 落库，避免后续返工。

## 验收标准（每项必须 ✅，量化）
- [ ] `db.get_session()` 上下文管理器，异常自动回滚
- [ ] `config.py` 读取 `STUDENT_DATA_DB_URL`，**新增** `KB_VECTOR_DB_URL`（如使用 pgvector / Chroma / Faiss）
- [ ] `kb_chunk` 表字段必须含：`chunk_id`(PK) / `doc_id` / `content` / `embedding`(vector 或 JSON) / `kp_tags`(JSON 数组) / `source_url` / `version`
- [ ] `test_profile` 表字段必须含：`profile_id`(PK) / `payload`(JSON) / `label`(字符串，可读名) / `expected_weak_kps`(JSON) / `created_at`
- [ ] 上述 4 张表都有建表 SQL（`models/*.py` 中定义 ORM）+ 单测确认能 CRUD
- [ ] 暴露模块级函数 `get_kb_chunk(chunk_id)` 给 D 的领域专家 Agent 调用
- [ ] 暴露模块级函数 `list_kb_chunks_by_kp(kp_id)` 给 A-05 quality_check 使用
- [ ] 单测：`db.py` 在测试环境下能连上，建表 SQL 可幂等执行
- [ ] 在群里公告 4 张表 schema 截图

## 一票否决
❌ 知识库切片表 / 测试画像表缺一 → 后续 B-05/B-06 无法开工，直接影响「实用价值 30 分」

---

# 任务 2：学情基础信息 + 核心指标

**ID**：B-01
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：B-00
**优先级**：P0
**归属目录**：`backend/2_学情数据/`

## 涉及文件
- `backend/2_学情数据/student/info.py`（新建）
- `backend/2_学情数据/student/metrics.py`（新建）
- `backend/2_学情数据/models/student.py`（新建）
- `backend/2_学情数据/tests/test_student_info.py`（新建）

## 描述
实现学情画像最常用的两个接口：基本信息（`/api/student/info`）和核心指标（`/api/student/metrics`）。**这两个接口的字段必须能被 B-04 聚合快照直接复用**，避免重复开发。

## 验收标准（严格）
- [ ] `GET /api/student/info?studentId=xxx` 返回 `api-doc §2.1` 完整字段 + **新增** `learnerProfile` 字段（含 `education` / `theoryTestScore` / `weakKPs` / `strongKPs`），给学情诊断 Agent 用
- [ ] `GET /api/student/metrics?studentId=xxx` 返回 `studyHours/completionRate/avgScore/trend/trendValue`
- [ ] 鉴权：仅本人 / 教师可看（调 A 的 `@require_auth`）
- [ ] studentId 不存在返回 404 + 明确 message
- [ ] **返回值 JSON 字段命名与 A-02 的 `learnerProfile` 完全一致**（统一从 A 调 `get_learner_profile`）
- [ ] 单测覆盖：正常 / 越权 / 不存在 / 字段完整性，**≥ 8 用例**

## 接口契约（关键 - D 必须按此结构取数）
```json
{
  "studentId": "s001",
  "name": "张三",
  "learnerProfile": {
    "education": "本科",
    "major": "智能制造",
    "theoryTestScore": 78,
    "weakKPs": ["kp12", "kp15"],
    "strongKPs": ["kp03"]
  },
  "metrics": {"studyHours": 32, "completionRate": 0.68, "avgScore": 82, "trend": "up", "trendValue": 0.05}
}
```

---

# 任务 3：能力维度 / 行为 / 知识掌握

**ID**：B-02
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：B-01
**优先级**：P1
**归属目录**：`backend/2_学情数据/`

## 涉及文件
- `backend/2_学情数据/student/dimensions.py`（新建）
- `backend/2_学情数据/student/behavior.py`（新建）
- `backend/2_学情数据/student/knowledge.py`（新建）
- `backend/2_学情数据/analytics/aggregator.py`（新建）— 周 / 月 / 学期聚合

## 验收标准
- [ ] `GET /api/student/dimensions?studentId=xxx` 返回 6 维能力雷达（**字段名固定**：`comprehension / application / analysis / evaluation / creation / collaboration`）
- [ ] `GET /api/student/behavior?studentId=xxx&period=week` 支持 `week/month/semester`
- [ ] `GET /api/student/knowledge?studentId=xxx` 返回 6 个知识模块进度（**字段名固定**：`kp_id / kp_name / mastery / status`）
- [ ] 行为数据缺失时按 0 填充，并加 `_isMock: true` 标注（MVP 阶段允许 mock）
- [ ] 单测覆盖三种 period 切换 + 字段命名一致性

---

# 任务 4：学习活动 5 个接口

**ID**：B-03
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：B-01
**优先级**：P0
**归属目录**：`backend/2_学情数据/`

## 涉及文件
- `backend/2_学情数据/activity/stats.py`（新建）
- `backend/2_学情数据/activity/courses.py`（新建）
- `backend/2_学情数据/activity/recent.py`（新建）
- `backend/2_学情数据/activity/calendar.py`（新建）
- `backend/2_学情数据/activity/record.py`（新建）
- `backend/2_学情数据/models/activity.py`（新建）

## 验收标准
- [ ] 5 个接口字段、参数严格按 `api-doc §5`
- [ ] `record` 接收后写入并返回新 id
- [ ] `calendar` 一周内每天一条，无数据补 0
- [ ] 单测覆盖 4 种 filter（`all/in-progress/completed/not-started`）

---

# 任务 5：暴露给 D 的聚合快照

**ID**：B-04
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：B-01, B-03
**优先级**：P1（必须早于 D-03 完成）
**归属目录**：`backend/2_学情数据/`

## 涉及文件
- `backend/2_学情数据/student/__init__.py`（扩展）
- `backend/2_学情数据/activity/__init__.py`（扩展）

## 描述
AI集成 层在拼 prompt 时需要一次拿到学生画像 + 最近活动。提供**模块级只读函数**供 D 调用。

## 验收标准
- [ ] `from backend.2_学情数据.student import get_student_snapshot` 可用
- [ ] `get_student_snapshot(studentId) -> dict` 返回 `info + metrics + dimensions + knowledge` 合并结果
- [ ] `from backend.2_学情数据.activity import get_recent_activities` 可用
- [ ] `get_recent_activities(studentId, days=7) -> list` 返回最近 N 天活动（按时间倒序）
- [ ] 函数**纯只读**，不写库
- [ ] 在 `backend/2_学情数据/概览.md` 中更新「对外约定」小节
- [ ] 单测：snapshot 字段完整、activities 时间倒序

---

# 任务 6：学情诊断 Agent ⭐ 夺奖核心

**ID**：B-05 ⭐ 夺奖专项
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：B-04
**优先级**：P0
**归属目录**：`backend/2_学情数据/`

## 涉及文件
- `backend/2_学情数据/analytics/diagnosis_agent.py`（新建）— 学情诊断 Agent 主体
- `backend/2_学情数据/analytics/diagnosis_prompts.py`（新建）— Agent 使用的 prompt 模板（顶部注释 `version: 0.1`）
- `backend/2_学情数据/tests/test_diagnosis_agent.py`（新建）
- `backend/2_学情数据/models/diagnosis_record.py`（新建）— 诊断结果历史表

## 描述
**挑战杯 3 个核心 Agent 之一**。学情诊断 Agent 的职责：拿 `get_student_snapshot` 的画像，结合任务/测试记录，**精准锚定**学习者的理论强项与技能盲区，输出结构化诊断结果。这是后续领域专家 Agent 的**唯一输入**。

## 验收标准（严格 - 一票否决项）
- [ ] 提供模块级函数 `diagnose(studentId) -> DiagnosisResult`：
  - 入参：studentId
  - 返回：`{studentId, weakKPs[], strongKPs[], knowledgeGaps[], confidence, traceId, generatedAt}`
  - `weakKPs` 必须与 A-02 的 `learnerProfile.weakKPs` **强相关但允许扩展**（Agent 可基于历史数据补全）
- [ ] **必须**通过 WebSocket 推送 `agent.thinking` 事件（调 A-04 的 `ws.emit(agentName='学情诊断Agent', step, content, traceId)`）
- [ ] **必须**保存诊断历史到 `diagnosis_record` 表（含 traceId / 完整 payload / generatedAt）
- [ ] **必须**调用 A 的 `公共/metrics.py` 中的 `calc_match_accuracy` 自我校验（与学习者画像自洽）
- [ ] `confidence` 字段值域 [0,1]；`< 0.6` 时必须抛 `QualityError`（由调用方决定是否降级）
- [ ] prompt 模板支持版本号，每次更新必须递增
- [ ] 单测覆盖：happy path / 弱知识画像 / 强知识画像 / 低 confidence 抛错 / 事件推送至少 1 次，**≥ 8 用例**
- [ ] 在 `backend/2_学情数据/概览.md` 中更新「对外约定」

## 接口契约（D 必看）
```python
from backend.2_学情数据.analytics import diagnose

result = diagnose(studentId="s001")
# 返回示例
# {
#   "studentId": "s001",
#   "weakKPs": ["kp12", "kp15"],
#   "strongKPs": ["kp03"],
#   "knowledgeGaps": [
#     {"kp_id": "kp12", "kp_name": "工业机器人坐标系", "severity": "high", "evidence": "..."}
#   ],
#   "confidence": 0.82,
#   "traceId": "trace-2026-08-15-001",
#   "generatedAt": "2026-08-15T10:00:00Z"
# }
```

## 一票否决
❌ 学情诊断 Agent 不能跑通 / 不能推送 `agent.thinking` 事件 → 完整性 30 分直接扣 5-15 分

---

# 任务 7：垂直领域知识库切片（≥1 个完整切片） ⭐ 夺奖核心

**ID**：B-06 ⭐ 夺奖专项
**负责人**：B（你）+ 全员配合
**状态**：⬜ 待开始
**依赖**：B-00
**优先级**：P0
**归属目录**：`backend/2_学情数据/kb/`（新建）

## 涉及文件
- `backend/2_学情数据/kb/source/`（新建）— 原始文档目录（PDF / Markdown / Word）
  - `README.md` — 知识库说明（领域、版本、来源、版权、chunk 数量、kp 列表）
- `backend/2_学情数据/kb/scripts/ingest.py`（新建）— 切片入库脚本
- `backend/2_学情数据/kb/scripts/eval_coverage.py`（新建）— **覆盖率自检脚本**（核心知识点覆盖率 ≥ 90%）
- `backend/2_学情数据/kb/chunks/`（新建）— 切片后的 JSONL 文件
- `backend/2_学情数据/kb/kp_taxonomy.json`（新建）— 知识点层级结构（树状）
- `backend/2_学情数据/tests/test_kb.py`（新建）

## 描述
**挑战杯硬性要求**：「至少 1 个垂直领域的专业知识库切片」。这是后续领域专家 Agent 检索的**唯一知识源**，直接决定「幻觉率 < 5%」和「核心知识点覆盖率 ≥ 90%」两项硬指标能否达标。

**推荐领域**（与教育/工业相关）：智能制造 / 工业互联网 / 工业机器人 / PLC 编程 / 智能传感器（任选 1 个，团队内部 7-15 协商决定后群里公告）

## 验收标准（一票否决项 - 严格量化）
- [ ] **≥ 1 个**完整的垂直领域知识库切片落地，覆盖**核心知识点数 ≥ 30 个**（`kp_taxonomy.json` 中定义）
- [ ] 切片总条数 **≥ 200 个**（每个 chunk 200-500 字）
- [ ] 每个 chunk 必须有 `kp_tags`（JSON 数组，挂载到 `kp_taxonomy.json` 的节点上）
- [ ] `ingest.py` 一键跑通：原始文档 → 切片 → 入 `kb_chunk` 表 → 可检索
- [ ] `eval_coverage.py` 跑通后输出：**核心知识点覆盖率**实际值（目标 ≥ 0.90）
- [ ] **必须有 1 份《知识库构建说明》** `kb/source/README.md`，含：领域、版本 v0.1、来源（注明是否原创 / 公开资料 / 授权）、chunk 数、kp 数、覆盖范围
- [ ] 单测：随机抽 5 个 kp，能从 `kb_chunk` 查到 ≥ 1 条记录
- [ ] 在群里公告：领域选择 + kp 数量 + chunk 数量 + 覆盖率自检结果
- [ ] **版权与合规**：使用公开资料必须标注来源；使用教材必须确保团队有使用权；**不**使用未授权商业资料

## 数据来源建议（合规优先）
- 公开教材：高职高专 / 本科公开课教材（注明来源）
- 国家标准 / 行业标准：GB / ISO / IEC 公开标准（节选关键条款）
- 团队原创：根据指导老师 / 企业专家访谈整理（推荐）
- Wikipedia / 百度百科：仅作为辅助，需二次加工

## 一票否决
❌ 知识库切片缺失 / kp 数 < 30 / chunk 数 < 200 / 覆盖率自检 < 0.90 → 实用价值 30 分全扣

---

# 任务 8：≥3 组差异化测试画像 + 预期弱知识 ⭐ 夺奖核心

**ID**：B-07 ⭐ 夺奖专项
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：B-05, B-06
**优先级**：P0
**归属目录**：`backend/2_学情数据/test_profiles/`（新建）

## 涉及文件
- `backend/2_学情数据/test_profiles/profile_01_本科应届生.json`
- `backend/2_学情数据/test_profiles/profile_02_高职在读生.json`
- `backend/2_学情数据/test_profiles/profile_03_企业转岗人员.json`
- `backend/2_学情数据/test_profiles/README.md`（**必读**）— 画像说明、使用方法、预期输出
- `backend/2_学情数据/test_profiles/expected_outputs/profile_01_expected.json`（**每组 1 份**）
- `backend/2_学情数据/test_profiles/expected_outputs/profile_02_expected.json`
- `backend/2_学情数据/test_profiles/expected_outputs/profile_03_expected.json`
- `backend/2_学情数据/scripts/load_test_profiles.py`（新建）— 把 JSON 入库到 `test_profile` 表
- `backend/2_学情数据/tests/test_profiles.py`（新建）

## 描述
**挑战杯硬性要求**：「不少于 2 组差异化的学习者初始学情数据源」，**我们做 3 组**（保险）且要覆盖 3 类典型背景。**每组必须**含完整输入画像 + 中间诊断数据 + 预期弱知识，用于 A-05 端到端跑 3 项硬指标。

## 验收标准（一票否决项 - 严格）
- [ ] **≥ 3 组**测试画像 JSON 文件，背景差异显著：
  - **画像 1（本科应届生）**：理论测试分 80+，期望 strong 偏多
  - **画像 2（高职在读生）**：理论测试分 60，weak 偏多
  - **画像 3（企业转岗人员）**：技能实战强，理论偏弱，**跨领域背景**（与画像 1/2 形成强对比）
- [ ] 每个画像 JSON 字段必须**完整**：
  - `learnerProfile`（含 education / major / theoryTestScore / weakKPs / strongKPs）
  - `activityHistory`（最近 7-30 天活动摘要）
  - `interactionGoal`（字符串，期望得到的资源类型）
- [ ] 每个画像配 1 份 `expected_*.json`：**人工标注**的预期弱知识清单（含 kp_id / severity / reason）
- [ ] `load_test_profiles.py` 一键导入到 `test_profile` 表
- [ ] `README.md` 必须含：3 组画像的差异点对比表 + 使用方法 + **每个画像的预期诊断结果**（**不少于 200 字说明**）
- [ ] 单测：3 组画像 JSON schema 校验通过 + 入库后能读出
- [ ] 群里公告：3 组画像差异点 + 预期诊断结果

## 一票否决
❌ 画像不足 3 组 / 画像差异不显著 / 缺预期输出 → 实用价值 30 分扣 5-10 分

---

# 任务 9：知识库与画像的覆盖率自检（**提前演练 A-05**）

**ID**：B-08
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：B-06, B-07
**优先级**：P0
**归属目录**：`backend/2_学情数据/`

## 涉及文件
- `backend/2_学情数据/scripts/coverage_check.py`（新建）
- `backend/2_学情数据/scripts/hallucination_check.py`（新建）

## 描述
**夺奖前最后一道关卡的预演**。在 A-05 之前，B 自己先跑一遍 3 项硬指标中的 2 项（覆盖率 + 幻觉率），提前发现知识库缺口。

## 验收标准
- [ ] `coverage_check.py` 输入：3 组测试画像 + 知识库；输出：核心知识点覆盖率
- [ ] `hallucination_check.py` 输入：3 组测试画像 + 知识库；输出：基于知识库检索的**潜在幻觉率预估**（用关键词匹配法）
- [ ] 两个脚本输出报告 `report_{ts}.json`，归档到 `docs/quality_reports/b_pre_check/`
- [ ] **覆盖率必须 ≥ 0.90**，不达标立即迭代知识库
- [ ] **幻觉率预估必须 < 0.10**（A-05 时再降到 < 0.05，留余量）

---

# 任务 10：参与 A-05 端到端验收

**ID**：B-09
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：A-05, B-05, B-06, B-07
**优先级**：P0
**归属目录**：`backend/2_学情数据/`

## 涉及文件
- `backend/2_学情数据/tests/test_e2e_quality.py`（新建）

## 验收标准
- [ ] 配合 A 跑通 A-05 的 `quality_check.py`，3 组画像全部走完
- [ ] 学情诊断 Agent 在 3 组画像上全部跑通
- [ ] 覆盖率 ≥ 0.90 / 幻觉率 < 0.05 / 适配准确率 ≥ 0.85
- [ ] 任一项不达标 → 阻塞提交，立即进入迭代

---

## 你的交付物清单（提交前自检）

- [ ] B-00 ~ B-09 全部 ✅
- [ ] 学情诊断 Agent 可独立调用，输出结构化结果 + 推送 WS 事件
- [ ] 知识库切片 ≥ 1 个 / kp ≥ 30 / chunk ≥ 200 / 覆盖率 ≥ 90%
- [ ] 3 组差异化测试画像 + 预期输出 全部入库
- [ ] `get_student_snapshot` / `get_recent_activities` / `diagnose` 模块级函数暴露
- [ ] B 的 `tests/` 覆盖率 ≥ 70%
- [ ] 知识库构建说明 + 测试画像 README 全部齐备
- [ ] 配合 A-05 跑通 3 项硬指标达标
