# 任务清单 — 成员 2：2_学情数据/

> **打开这一份就够了**。本文件包含你（成员 2/B）负责的所有任务，按 1/2/3 顺序排好。
> 目录约定见 `backend/概览.md`；协作规范见 `backend/协作协议.md`；状态总览见 `backend/任务总看板.md`。

---

# 任务 1：2_学情数据/ 自有数据层

**ID**：B-00
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：无
**优先级**：P0
**归属目录**：`backend/2_学情数据/`

## 涉及文件
- `backend/2_学情数据/db.py` (新建)
- `backend/2_学情数据/config.py` (新建，读取 `STUDENT_DATA_DB_URL` 等本区专属配置)
- `backend/2_学情数据/models/` (目录已建)

## 描述
学情数据 自有的数据库 / 配置 / ORM。**不放**在 `公共/`，避免与 A/C/D 撞车。

## 验收标准
- [ ] `db.py` 提供本区数据库连接（连接池、会话上下文管理器 `get_session()`）
- [ ] `config.py` 从环境变量读取 `STUDENT_DATA_DB_URL`
- [ ] ORM 模型放在 `models/` 子目录下（`models/student.py`、`models/activity.py` 等）
- [ ] 本区所有后续任务都依赖本任务
- [ ] 单元测试：`db.py` 在测试环境下能连上
- [ ] 在群里公告 `db.get_session()` 接口签名
- [ ] 把 `STUDENT_DATA_DB_URL` 加入项目根 `.env.example` 模板

---

# 任务 2：学情基础信息 + 核心指标

**ID**：B-01
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：B-00
**优先级**：P0
**归属目录**：`backend/2_学情数据/`

## 涉及文件
- `backend/2_学情数据/student/info.py` (新建)
- `backend/2_学情数据/student/metrics.py` (新建)
- `backend/2_学情数据/models/student.py` (新建)
- `backend/2_学情数据/tests/test_student_info.py` (新建)

## 描述
实现学情画像最常用的两个接口：基本信息（`/api/student/info`）和核心指标（`/api/student/metrics`）。

## 验收标准
- [ ] `GET /api/student/info?studentId=xxx` 返回 `api-doc §2.1` 完整字段
- [ ] `GET /api/student/metrics?studentId=xxx` 返回 `studyHours/completionRate/avgScore/trend/trendValue`
- [ ] 鉴权：仅本人 / 教师可看
- [ ] studentId 不存在返回 404
- [ ] 单测覆盖正常 / 越权 / 不存在

---

# 任务 3：能力维度 / 行为 / 知识掌握

**ID**：B-02
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：B-01
**优先级**：P1
**归属目录**：`backend/2_学情数据/`

## 涉及文件
- `backend/2_学情数据/student/dimensions.py` (新建)
- `backend/2_学情数据/student/behavior.py` (新建)
- `backend/2_学情数据/student/knowledge.py` (新建)
- `backend/2_学情数据/analytics/aggregator.py` (新建，周 / 月 / 学期聚合)

## 验收标准
- [ ] `GET /api/student/dimensions?studentId=xxx` 返回 6 维能力雷达
- [ ] `GET /api/student/behavior?studentId=xxx&period=week` 支持 week/month/semester
- [ ] `GET /api/student/knowledge?studentId=xxx` 返回 6 个知识模块进度
- [ ] 行为数据缺失时按 0 填充，并加 `_isMock: true` 标注（MVP 阶段允许 mock）
- [ ] 单测覆盖三种 period 切换

---

# 任务 4：学习活动 5 个接口

**ID**：B-03
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：B-01
**优先级**：P0
**归属目录**：`backend/2_学情数据/`

## 涉及文件
- `backend/2_学情数据/activity/stats.py` (新建)
- `backend/2_学情数据/activity/courses.py` (新建)
- `backend/2_学情数据/activity/recent.py` (新建)
- `backend/2_学情数据/activity/calendar.py` (新建)
- `backend/2_学情数据/activity/record.py` (新建)
- `backend/2_学情数据/models/activity.py` (新建)

## 验收标准
- [ ] 5 个接口字段、参数严格按 `api-doc §5`
- [ ] `record` 接收后写入并返回新 id
- [ ] `calendar` 一周内每天一条，无数据补 0
- [ ] 单测覆盖 4 种 filter（all/in-progress/completed/not-started）

---

# 任务 5：暴露给 D 的聚合快照

**ID**：B-04
**负责人**：B（你）
**状态**：⬜ 待开始
**依赖**：B-01, B-03
**优先级**：P1
**归属目录**：`backend/2_学情数据/`

## 涉及文件
- `backend/2_学情数据/student/__init__.py` (扩展)
- `backend/2_学情数据/activity/__init__.py` (扩展)

## 描述
AI集成 层在拼 prompt 时需要一次拿到学生画像 + 最近活动。提供两个**模块级只读函数**供 D 调用。

## 验收标准
- [ ] `from backend.2_学情数据.student import get_student_snapshot` 可用
- [ ] `get_student_snapshot(studentId) -> dict` 返回 `info + metrics + dimensions + knowledge` 合并结果
- [ ] `from backend.2_学情数据.activity import get_recent_activities` 可用
- [ ] `get_recent_activities(studentId, days=7) -> list` 返回最近 N 天活动
- [ ] 函数纯只读，不写库
- [ ] 在 `backend/2_学情数据/概览.md` 中更新"对外约定"小节
- [ ] 单测：snapshot 字段完整、activities 时间倒序
