# 任务清单 — 成员 3：3_学习内容/

> **打开这一份就够了**。本文件包含你（成员 3/C）负责的所有任务，按 1/2/3 顺序排好。
> 目录约定见 `backend/概览.md`；协作规范见 `backend/协作协议.md`；状态总览见 `backend/任务总看板.md`。

---

# 任务 1：3_学习内容/ 自有数据层

**ID**：C-00
**负责人**：C（你）
**状态**：⬜ 待开始
**依赖**：无
**优先级**：P0
**归属目录**：`backend/3_学习内容/`

## 涉及文件
- `backend/3_学习内容/db.py` (新建)
- `backend/3_学习内容/config.py` (新建，读取 `LEARNING_CONTENT_DB_URL` 等)
- `backend/3_学习内容/models/` (目录已建)

## 描述
学习内容 自有的数据库 / 配置 / ORM。**不放**在 `公共/`，避免与 A/B/D 撞车。

## 验收标准
- [ ] `db.py` 提供本区数据库连接
- [ ] `config.py` 从环境变量读取 `LEARNING_CONTENT_DB_URL`
- [ ] ORM 模型放在 `models/` 子目录下
- [ ] 本区所有后续任务都依赖本任务
- [ ] 单元测试：`db.py` 在测试环境下能连上
- [ ] 在群里公告 `db.get_session()` 接口签名
- [ ] 把 `LEARNING_CONTENT_DB_URL` 加入项目根 `.env.example` 模板

---

# 任务 2：学习路径 4 个接口

**ID**：C-01
**负责人**：C（你）
**状态**：⬜ 待开始
**依赖**：C-00
**优先级**：P0
**归属目录**：`backend/3_学习内容/`

## 涉及文件
- `backend/3_学习内容/learning_path/overview.py` (新建)
- `backend/3_学习内容/learning_path/timeline.py` (新建)
- `backend/3_学习内容/learning_path/modules.py` (新建)
- `backend/3_学习内容/learning_path/tasks.py` (新建)
- `backend/3_学习内容/models/learning_path.py` (新建)
- `backend/3_学习内容/tests/test_path.py` (新建)

## 描述
实现学习路径模块的 4 个非 AI 接口。

## 验收标准
- [ ] `GET /api/learning-path/overview` 字段对齐 `api-doc §3.1`
- [ ] `GET /api/learning-path/timeline` 支持 status 过滤（completed/current/pending）
- [ ] `GET /api/learning-path/modules` 返回模块 + 进度
- [ ] `GET /api/learning-path/tasks?studentId=xxx` 返回今日任务清单
- [ ] 单测覆盖 timeline 3 种状态切换

---

# 任务 3：学习建议 2 个接口

**ID**：C-02
**负责人**：C（你）
**状态**：⬜ 待开始
**依赖**：C-01
**优先级**：P0
**归属目录**：`backend/3_学习内容/`

## 涉及文件
- `backend/3_学习内容/suggestions/list.py` (新建)
- `backend/3_学习内容/suggestions/read.py` (新建)
- `backend/3_学习内容/models/suggestion.py` (新建)

## 验收标准
- [ ] `GET  /api/suggestions/list?studentId&category` 支持 all/method/resource/review/practice 5 种过滤
- [ ] `POST /api/suggestions/read` 入参 `{studentId, suggestionId}` 返回 `{success: true}`
- [ ] 单测覆盖 5 种 category 过滤

---

# 任务 4：暴露给 D 的写回方法

**ID**：C-03
**负责人**：C（你）
**状态**：⬜ 待开始
**依赖**：C-01, C-02
**优先级**：P1
**归属目录**：`backend/3_学习内容/`

## 涉及文件
- `backend/3_学习内容/learning_path/__init__.py` (扩展)
- `backend/3_学习内容/suggestions/__init__.py` (扩展)

## 描述
D 在生成 AI 内容后要写回本区数据表。提供两个**模块级写函数**，D 通过 import 调用，**不**直接操作 C 的 ORM。

## 验收标准
- [ ] `from backend.3_学习内容.learning_path import save_ai_generated_path` 可用
- [ ] `save_ai_generated_path(studentId: str, content: str) -> None` 写入并保留历史版本
- [ ] `from backend.3_学习内容.suggestions import save_ai_generated_suggestions` 可用
- [ ] `save_ai_generated_suggestions(studentId: str, content: str) -> None` 写入
- [ ] 在 `backend/3_学习内容/概览.md` 中更新"写回约定"小节
- [ ] 单测：写后 read 能拿到
