# 3_学习内容/ — 成员 C：学习内容（路径 / 建议）

> **不含 AI 生成部分**。`/api/learning-path/ai-result`、`/api/suggestions/ai-result` 归成员 D，避免冲突。

## 负责接口

- 学习路径：
  - `GET  /api/learning-path/overview`
  - `GET  /api/learning-path/timeline`
  - `GET  /api/learning-path/modules`
  - `GET  /api/learning-path/tasks`
- 学习建议：
  - `GET  /api/suggestions/list`
  - `POST /api/suggestions/read`

## 不归本区（务必注意）

- `POST /api/learning-path/ai-result`  → D
- `GET  /api/learning-path/ai-result`  → D
- `POST /api/suggestions/ai-result`    → D
- `/api/ai-chat/*`                     → D

## 建议文件结构

```
3_学习内容/
├── 概览.md
├── db.py                  # 本区自有 DB 连接
├── config.py              # 本区自有配置（LEARNING_CONTENT_DB_URL）
├── routes.py
├── learning_path/
│   ├── overview.py
│   ├── timeline.py
│   ├── modules.py
│   ├── tasks.py
│   └── __init__.py        # 暴露 save_ai_generated_path 给 D
├── suggestions/
│   ├── list.py
│   ├── read.py
│   └── __init__.py        # 暴露 save_ai_generated_suggestions 给 D
├── models/                # ORM 按表分文件
│   ├── learning_path.py
│   └── suggestion.py
└── tests/
    └── test_path.py
```

> C 的任务清单已统一放到 backend 根目录的 `任务清单_3_学习内容.md`，本目录不再保留任务清单。

## 依赖 / 被依赖

- **依赖**：A（userId 鉴权）、B（学情快照，用于个性化排序 / 过滤）
- **被依赖**：D 在生成路径 / 建议后，会**写回**到本区数据表（通过本区暴露的方法，不要直接动本区的文件 / 数据库）

## 数据库归属

本区拥有独立的数据库（`LEARNING_CONTENT_DB_URL` 指向），A / B / D 任何代码**不可**直连本区数据库，只能调用本区暴露的模块级函数。

## 写回约定（C 暴露给 D 的内部方法）

```python
# learning_path/__init__.py 模块级
def save_ai_generated_path(studentId: str, content: str) -> None: ...

# suggestions/__init__.py 模块级
def save_ai_generated_suggestions(studentId: str, content: str) -> None: ...
```

D 通过 `from backend.3_学习内容.learning_path import save_ai_generated_path` 调用。**D 不直接读写本区数据表**。
