# 00 — 项目全局启动提示词（**所有 AI 必读入口**）

> 把这一整段**原样复制**发给你的 AI 编程助手（Claude / Cursor / Trae / 通义灵码 / 智谱 GLM / …），它就会按下面的流程把项目全貌吃透。

---

```
你是本项目「XH-202630 挑战杯 — 领域知识个性化生成与多智能体协同决策系统」后端开发的 AI 协作者。**目标：夺擂主奖（12 万元）**。在写任何代码前，请按以下顺序**完整阅读**所有相关文档，每读一份回复「✅ 已读 <文件名>」。

# 📚 第一步：必读文档（按顺序）

1. backend/协作协议.md — 工作协议、夺奖红线、3 项硬指标
2. backend/AI协作指南.md — AI 协作者工作指南（含 Agent 协作）
3. backend/概览.md — 分工总览、夺奖重点、时间线倒推
4. backend/任务总看板.md — 跨区任务总览 + 9 个挑战杯里程碑
5. backend/prompts/README.md — 提示词库索引（**让 AI 自己能检索架构的关键**）
6. **告诉我你是哪个成员（A / B / C / D）**，我会再让你读你专属的提示词 + 任务清单

# 🚨 第二步：3 项硬指标红线（一票否决项）

**提交作品（2026-09-05）前必须全部达标**：

| 指标 | 目标 | 唯一计算函数 |
| --- | --- | --- |
| 专业知识谬误率（幻觉率） | **< 5%** | `backend.公共.metrics.calc_hallucination_rate` |
| 画像-难度适配准确率 | **≥ 85%** | `backend.公共.metrics.calc_match_accuracy` |
| 核心知识点覆盖率 | **≥ 90%** | `backend.公共.metrics.calc_coverage` |

🚫 禁止你自己重写指标函数，**必须**只从 `backend.公共.metrics` import。

# 🤖 第三步：多智能体协作模式（**项目核心**）

```
B-05 学情诊断 Agent ─┐
                    ├─→ D-03 协同编排器 ─→ C-04 领域专家 Agent ─→ D-06 审核裁判 Agent ─→ 决策
A-04 WebSocket 事件 ─┘                                          │
                    └──────── audit.score < 0.85 时触发辩论 ──┘
```

每个 Agent 的硬要求：
- ✅ 通过 A-04 的 `ws.emit(agentName, event_type, ...)` 推送 5 类事件
- ✅ 落 `agent_log` 表（D-00 提供 ORM，禁止直接 INSERT）
- ✅ 调 `公共/metrics.*` 自检 3 项指标
- ✅ 保留 traceId 串联到下一个 Agent

5 类事件类型：
| type | 触发时机 |
| --- | --- |
| `agent.start` | Agent 启动时 |
| `agent.thinking` | Agent 处理中（可多次） |
| `agent.result` | Agent 完成时 |
| `agent.debate` | 审核不达标触发辩论时 |
| `agent.final` | 整个协同流程结束 |

# 📦 第四步：9-5 必交材料清单（缺一不可）

- 源码 + 部署说明 + 单元测试
- ≥ 1 个垂直领域知识库切片（B-06）
- ≥ 3 组差异化学习者画像（B-07）
- ≤ 10 分钟演示视频（D-08）
- 15-20 页 PPT（D-09）

# ⏰ 第五步：时间线（**生死线**）

| 截止 | 节点 | 负责人 |
| --- | --- | --- |
| 2026-06-30 | M1 地基就绪 | A |
| 2026-07-20 | M2 数据层 + 基础接口 | B/C |
| **2026-08-15** | **M3 ⭐ 3 大 Agent 全部跑通 + M4 ⭐ 知识库达标** | B/C/D |
| **2026-08-22** | **M5 ⭐ 3 项硬指标首次达标** | A + 全员 |
| 2026-09-01 | M6 视频初剪 + M7 PPT 初版 | D |
| **2026-09-03** | **M8 ⭐ 全部材料就绪** | 全员 |
| **2026-09-05** | **M9 作品提交** | 队长 |

# 🗂️ 第六步：4 个区物理隔离

```
backend/
├── 公共/                # A 负责（response/logger/errors/config/metrics/quality_check）
├── 1_用户与聊天/         # A（地基 + WebSocket + 学习者画像）
├── 2_学情数据/           # B（学情 + 诊断 Agent + 知识库 + 测试画像）
├── 3_学习内容/           # C（路径 + 建议 + 领域专家 + 渲染 + 迭代）
└── 4_AI集成/             # D（4 AI + 编排 + 审核 + 可视化 + 视频 + PPT）
```

🚫 **禁止跨区修改文件**；跨区调用**必须**走对方暴露的模块级函数。

# ✅ 第七步：阅读完毕请回答

读完上述文档后，请明确回答：
1. 你理解了项目的 **3 项硬指标红线** 吗？请复述
2. 你理解了 **3 大 Agent 协同闭环** 吗？请画出流程
3. 你理解了 **文件所有权红线** 吗？你会修改 `公共/` 吗？
4. 你的工作范围是什么？会动哪些目录？
5. 你遇到错误时，按 `backend/AI协作指南.md §10 出错处理` 的哪条规则处理？

回答完毕后，请说"✅ 全局启动完成，等待分配任务"，然后我会告诉你：
- 你是哪个成员
- 你当前要做的任务 ID 和标题
- 你专属的提示词（`0X_成员X_xxx_提示词.md`）和该任务的专项提示词
```

---

## 给项目负责人的话

- **新成员加入 / 新 AI 协作者** → 把上面那段发给它即可，**无需人工讲解**
- **任何成员开工新任务** → 让 AI 先读完本文件 + 自己的成员提示词 + 任务清单，再开工
- **定期 Review** → 让 AI 复述「3 项硬指标 + 3 大 Agent 协同 + 9-5 必交材料」作为开工门槛
