# A 区进度交接文档（截至 2026-08-12）

> **目的**：方便新会话/新成员快速接手 A 区当前进度。
> **位置**：`d:\TZB\TZB-XH202630\A区进度交接_2026-08-12.md`
> **项目**：挑战杯 XH-202630 "领域知识个性化生成与多智能体协同决策系统研究"
> **总目标**：4 个区（公共/A/B/C/D）协同 + 3 个 Agent 协同 + 端到端验收

---

## 1. 一句话总览

**A 区（用户与聊天）核心 4 任务已全部完成并通过测试**：
- A-00 数据层 + A-01 鉴权 + A-02 用户/画像 + A-04 WebSocket
- **45 个单元测试全过 + 端到端验证通过**
- 服务可在 `http://127.0.0.1:8000` 启动，Swagger UI 入口 `/docs`

---

## 2. 环境信息

| 项目 | 值 |
|------|-----|
| Python | 3.13.14 (WindowsApps) |
| venv 路径 | `D:\TZB\TZB-XH202630\venv\` |
| 数据库 | MySQL 8.0 (localhost:3306) |
| 数据库名 | `tzb_user_chat` |
| 密码 | `L35714776`（已写入项目根 `.env`，**已 gitignore**）|
| Web 框架 | FastAPI + uvicorn |
| ORM | SQLAlchemy 2.0 async + aiomysql |
| 测试框架 | pytest + pytest-asyncio + httpx |

### 启动服务

```powershell
cd D:\TZB\TZB-XH202630
.\venv\Scripts\Activate.ps1
python -m uvicorn backend.main:app --reload --port 8000
```

打开浏览器访问：
- **Swagger UI**：[http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs) — 所有接口可视化测试
- **健康检查**：[http://127.0.0.1:8000/health](http://127.0.0.1:8000/health)
- **根路径**：[http://127.0.0.1:8000/](http://127.0.0.1:8000/)

---

## 3. 已完成任务清单

| ID | 名称 | 状态 | 优先级 | 测试数 |
|----|------|------|--------|--------|
| S-01 | 公共基础工具 + 3 项硬指标计算器 | ✅ | P0 | 22 |
| A-00 | 数据层（建库 + 建表 + 种子） | ✅ | P0 | - |
| A-01 | 用户注册/登录/登出（bcrypt + JWT） | ✅ | P0 | 9 |
| A-02 | 用户信息 + 学习者画像 | ✅ | P0 | 13 |
| A-04 | WebSocket 实时通道 | ✅ | P0⭐ | 10 |
| A-03 | 聊天消息 4 接口 | ⬜ 未开始 | P1 | - |

**合计**：4 个 P0 任务完成，**45 个单测全过**。

---

## 4. 已实现接口清单

### 鉴权（A-01）
- `POST /api/auth/register` — 用户注册
- `POST /api/auth/login` — 用户登录，返回 JWT
- `POST /api/auth/logout` — 用户登出，token 加入黑名单

### 用户/画像（A-02）
- `GET  /api/user/info?userId=xxx` — 读用户信息（User + LearnerProfile 合并）
- `PUT  /api/user/profile` — 更新学习者画像

### WebSocket（A-04）
- `WS /ws?token=xxx` — 实时通道，支持：
  - 客户端消息 `{type: "ping"}` → 回 `{type: "pong"}`
  - 客户端消息 `{type: "subscribe", channel: "xxx"}` → 订阅频道（不回包）
  - 客户端消息 `{type: "unsubscribe", channel: "xxx"}` → 取消订阅
  - 客户端消息 `{type: "chat", targetId: "u002", content: "..."}` → 推送给 target

### 系统
- `GET  /` — 根路径
- `GET  /health` — 健康检查（含 DB + WS 状态）

---

## 5. 测试账号（种子数据）

| 用户名 | 密码 | ID | 角色 | 画像 |
|--------|------|-----|------|------|
| `student001` | `Test@1234` | `u001` | student | ✅ 有 |
| `student002` | `Test@1234` | `u002` | student | ✅ 有 |
| `teacher001` | `Test@1234` | `t001` | teacher | ❌ 无 |
| `admin001`   | `Test@1234` | `a001` | admin   | ❌ 无 |

**重置种子**：

```powershell
cd D:\TZB\TZB-XH202630
.\venv\Scripts\Activate.ps1
python -m backend.a_用户与聊天.seed_data
```

**重置用户画像到 seed 原始值**（测试隔离用）：

```powershell
python reset_test_data.py   # 项目根目录下
```

---

## 6. 数据库结构（A 区自有）

数据库：`tzb_user_chat`（utf8mb4）

### 表 1：`user`
| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | VARCHAR(64) PK | 用户 ID（u001/u002/t001/a001）|
| `username` | VARCHAR(64) UNIQUE | 登录名 |
| `password_hash` | VARCHAR(255) | bcrypt 哈希 |
| `name` | VARCHAR(64) | 显示名 |
| `role` | VARCHAR(16) | student/teacher/admin |
| `created_at` | DATETIME | 注册时间 |

### 表 2：`learner_profile`
| 字段 | 类型 | 说明 |
|------|------|------|
| `user_id` | VARCHAR(64) PK + FK(user.id) | 关联用户 |
| `education` | VARCHAR(32) | 学历 |
| `major` | VARCHAR(64) | 专业 |
| `theory_test_score` | INT | 理论测试分（0-100）|
| `weak_kps` | JSON | 薄弱知识点 ID 列表（**其他区只读**）|
| `strong_kps` | JSON | 擅长知识点 ID 列表（**其他区只读**）|
| `updated_at` | DATETIME | 最近更新时间（自动 onupdate）|

---

## 7. 模块文件结构（A 区已实现部分）

```
backend/
├── __init__.py
├── main.py                            # FastAPI 启动入口（已挂载 auth/user/ws 三个路由）
├── requirements.txt                   # 依赖：fastapi/sqlalchemy/aiomysql/passlib/bcrypt/...
├── .env.example                       # 配置模板（含 USER_CHAT_DB_URL 等）
│
├── 公共/                              # S-01 ✅
│   ├── config.py                      # 基础配置（**不含** DB_URL）
│   ├── errors.py                      # BizError/AuthError/ForbiddenError/NotFoundError/AgentError/QualityError
│   ├── response.py                    # 统一 {code, message, data} 响应
│   ├── logger.py                      # loguru 封装
│   ├── metrics.py                     # 3 项硬指标计算器（hallucination/match_accuracy/coverage）
│   ├── __init__.py
│   └── tests/                         # 22 个单测
│
├── a_用户与聊天/                      # A 区
│   ├── config.py                      # A 区私有配置（读 USER_CHAT_DB_URL）
│   ├── db.py                          # 引擎 + Session + 跨区业务函数
│   ├── init_db.py                     # 一键建库 + 建表
│   ├── seed_data.py                   # 种子数据（4 个测试账号）
│   ├── 启动指南.md
│   │
│   ├── models/                        # ORM 模型
│   │   ├── base.py
│   │   ├── user.py
│   │   └── learner_profile.py
│   │
│   ├── auth/                          # A-01 ✅
│   │   ├── __init__.py
│   │   ├── passwords.py               # bcrypt 哈希/验证/强度校验
│   │   ├── tokens.py                  # JWT 创建/解析
│   │   ├── blacklist.py               # token 黑名单
│   │   ├── schemas.py
│   │   ├── register.py                # POST /api/auth/register
│   │   ├── login.py                   # POST /api/auth/login
│   │   ├── logout.py                  # POST /api/auth/logout
│   │   └── router.py
│   │
│   ├── user/                          # A-02 ✅
│   │   ├── __init__.py
│   │   ├── deps.py                    # get_current_user 公共依赖
│   │   ├── schemas.py                 # UserInfoResponse / UserProfileRequest
│   │   ├── info.py                    # GET /api/user/info
│   │   ├── profile.py                 # PUT /api/user/profile
│   │   └── router.py
│   │
│   ├── ws/                            # A-04 ✅
│   │   ├── __init__.py
│   │   ├── server.py                  # WebSocket 端点
│   │   ├── manager.py                 # 连接管理器（订阅/广播/事件缓冲）
│   │   ├── handlers.py                # 客户端消息处理器
│   │   ├── events.py                  # Agent 协同事件协议
│   │   └── README.md
│   │
│   └── tests/                         # 32 个单测（auth 9 + db 13 + ws 10 + profile 13）
│       ├── test_auth.py
│       ├── test_db.py
│       ├── test_ws.py
│       └── test_profile.py            # A-02 新增
│
├── b_学情数据/                        # B 区（未开始）
├── c_学习内容/                        # C 区（未开始）
└── d_AI集成/                          # D 区（未开始）
```

---

## 8. 关键设计决策

### 8.1 数据库隔离
**每个区有自己独立的 MySQL 数据库**，通过模块级函数访问：

```python
# A 区
USER_CHAT_DB_URL=mysql+aiomysql://root:xxx@localhost:3306/tzb_user_chat

# B 区（占位）
STUDENT_DATA_DB_URL=mysql+aiomysql://root:xxx@localhost:3306/tzb_student_data
# C 区（占位）
LEARNING_CONTENT_DB_URL=mysql+aiomysql://root:xxx@localhost:3306/tzb_learning_content
# D 区（占位）
AI_INTEGRATION_DB_URL=mysql+aiomysql://root:xxx@localhost:3306/tzb_ai_integration
```

**跨区数据访问必须通过 A 区的模块级函数**（在 `db.py`）：

```python
# B/C/D 读 A 的数据
from backend.a_用户与聊天.db import get_user_by_id, get_learner_profile, upsert_learner_profile

user = await get_user_by_id("u001")
profile = await get_learner_profile("u001")
await upsert_learner_profile("u001", weak_kps=["kp12", "kp15"])
```

### 8.2 异常体系
- `BizError` (基类) → HTTP 400
- `AuthError` → 401（缺/错 token）
- `ForbiddenError` → 403（**已登录但越权**）
- `NotFoundError` → 404
- `QualityError` → 422（3 项硬指标不达标）
- `AgentError` → 500（Agent 调度失败）

### 8.3 鉴权依赖
所有需要登录的接口都用 `Depends(get_current_user)`：

```python
from backend.a_用户与聊天.user.deps import get_current_user

@router.get(...)
async def my_endpoint(current: dict = Depends(get_current_user)):
    user_id = current["sub"]     # 用户 ID
    role = current["role"]       # student/teacher/admin
    name = current["name"]       # 显示名
```

### 8.4 越权规则（A-02 实现）
- **读** `/api/user/info`：
  - `self` → 200
  - `teacher/admin` → 200（任何用户）
  - 其他（学生读他人）→ **403**
- **写** `/api/user/profile`：
  - `teacher/admin` → 可指定 `?userId=xxx` 改任意用户
  - `student/其他` → 只能改自己（省略 `?userId` 时改自己；指定他人 → **403**）

### 8.5 ⚠️ MySQL REPEATABLE READ 坑（已修复）
**问题**：在 `get_session()` 上下文内调用另一个 `get_session()` 函数读数据，新 session 看到的是事务开始时的快照，看不到外层 flush 的数据。

**修复**（`db.py:upsert_learner_profile`）：flush 后**直接在同一 session 内**读，不用嵌套 session。

**新写 B/C/D 时记住**：如果一个函数要写后立刻读最终值，**必须在同一 session 内**读。

---

## 9. Git 工作流

### 当前分支
- `feat/S-公共基础工具` ✅（已推 origin）
- `feat/A-00-数据层` ✅（已推 origin）
- `feat/A-数据层` ✅（已推 origin，含 A-00/A-01/A-04）
- **A-02 还没提交** ⚠️

### 提交 A-02 的命令

```powershell
cd D:\TZB\TZB-XH202630
git status
git add backend/a_用户与聊天/user/ `
        backend/a_用户与聊天/db.py `
        backend/a_用户与聊天/models/ `
        backend/a_用户与聊天/seed_data.py `
        backend/a_用户与聊天/tests/test_profile.py `
        backend/公共/errors.py `
        backend/main.py `
        backend/任务清单_a_用户与聊天.md
# ⚠️ 不要 add .env / debug_routes.py / test_ws_manual.py / reset_test_data.py

# 确认 .env 没被 add
git status

# 提交
git commit -m "feat(A-02): 用户信息 + 学习者画像读写 + 越权控制"

# 推送到 origin
git push -u origin <branch-name>
```

### PR 流程
看 `backend/workflows/14_Git提交与PR流程.md`

---

## 10. 下一步推荐

### 选项 A：A-03 聊天消息 4 接口（P1）
按 `backend/任务清单_a_用户与聊天.md` 任务 6：
- `POST /api/chat/send` — 发消息（type: text/image/file）
- `GET  /api/chat/history` — 历史消息（分页）
- `GET  /api/chat/list` — 会话列表
- `POST /api/chat/read` — 标记已读
- 新增 `backend/a_用户与聊天/models/message.py`
- 4 个新接口 + 单元测试

### 选项 B：辅助工作
- refactor `auth/logout.py` 用 `get_current_user`（消除重复）
- 添加 `conftest.py` 做测试隔离（避免 `reset_test_data.py` 手动重置）
- 写 OpenAPI 文档（api-doc.js 对齐）

### 选项 C：等 B/C/D 启动后再回来补 A-03
因为 A-03 聊天是 P1，且依赖 B 的会话概念。

---

## 11. 已知问题 / 注意事项

1. **PowerShell 中文乱码**：curl 输出中文乱码是 PowerShell 默认 GBK 编码问题，**服务端 JSON 是 UTF-8 正常**。建议用 Swagger UI 测试。
2. **TRAE 沙箱的 Python**：和 venv 不一样，沙箱**没有 aiomysql**。所有数据库/uvicorn 命令都要在用户自己的 PowerShell 跑。
3. **TRAE 沙箱访问不了 GitHub 443**：所有 `git push` 都要手动跑。
4. **`reset_test_data.py`** 是测试隔离临时脚本，**不应该提交**到 git（暂时在项目根）。
5. **MySQL `user.id` 是 VARCHAR 而非 INT**：保留为字符串 ID（如 "u001"），方便日志调试。
6. **学习者画像的 weak_kps/strong_kps 是 JSON 数组**，存知识点 ID 字符串列表。其他区**只读**这两个字段。

---

## 12. 跨区协作约定（B/C/D 必看）

### A 区对外暴露
```python
# 1. 读用户基础信息
from backend.a_用户与聊天.db import get_user_by_id
user = await get_user_by_id("u001")  # → {userId, name, role} 或 None

# 2. 读学习者画像
from backend.a_用户与聊天.db import get_learner_profile
profile = await get_learner_profile("u001")  
# → {education, major, theoryTestScore, weakKPs, strongKPs, updatedAt} 或 None

# 3. 写/更新学习者画像
from backend.a_用户与聊天.db import upsert_learner_profile
await upsert_learner_profile("u001", weak_kps=["kp12"], theory_test_score=80)

# 4. 鉴权依赖（GET 请求想拿当前用户）
from backend.a_用户与聊天.user.deps import get_current_user
# 用法：current: dict = Depends(get_current_user)
```

### B 的学情诊断 Agent 回写流程
1. B 通过 `get_learner_profile(user_id)` 读画像
2. 诊断后通过 `upsert_learner_profile(user_id, weak_kps=[...])` 回写
3. **不要** 直接写 DB

### A 的 WebSocket 推送约定
A-04 的 `connection_manager.broadcast_to_channel(channel, event)` 可以给订阅某个 channel 的所有用户推事件。Agent 协同事件推送到 `agent:xxx` 频道（具体协议在 `ws/events.py`）。

---

## 13. 任务清单位置

- **A 区任务清单**：`backend/任务清单_a_用户与聊天.md`（**A-02 已标 ✅**）
- **总看板**：`backend/任务总看板.md`
- **协作协议**：`backend/协作协议.md`
- **A 区概览**：`backend/a_用户与聊天/概览.md`
- **API 契约**（前端用）：`api-doc.js`（**项目根**，注意不是 backend 目录下）

---

## 14. 启动 + 测试 + 验收 速查

```powershell
# === 一键启动 ===
cd D:\TZB\TZB-XH202630
.\venv\Scripts\Activate.ps1

# 1. 建库建表（首次或 schema 改了才需要）
python -m backend.a_用户与聊天.init_db

# 2. 写种子数据
python -m backend.a_用户与聊天.seed_data

# 3. 跑所有测试
python -m pytest backend/a_用户与聊天/tests/ -v

# 4. 启服务
python -m uvicorn backend.main:app --reload --port 8000

# === 测试隔离（如果 test_db.py 因为 test_profile.py 改了状态而失败） ===
python reset_test_data.py
```

---

## 15. 当前文件改动清单（A-02 未提交部分）

```
M  backend/main.py                                       # 挂载 user_router
M  backend/公共/errors.py                                 # 新增 ForbiddenError
M  backend/a_用户与聊天/db.py                             # 修复 upsert REPEATABLE READ
M  backend/a_用户与聊天/seed_data.py                      # 新增 admin001
M  backend/任务清单_a_用户与聊天.md                       # A-02 标完成

A  backend/a_用户与聊天/user/__init__.py
A  backend/a_用户与聊天/user/deps.py
A  backend/a_用户与聊天/user/schemas.py
A  backend/a_用户与聊天/user/info.py
A  backend/a_用户与聊天/user/profile.py
A  backend/a_用户与聊天/user/router.py
A  backend/a_用户与聊天/tests/test_profile.py
```

---

**下次工作流开始时，先读这个文档的 §1-§3、§10、§12，就能快速进入状态。**

**最后更新**：2026-08-12 01:45 (Asia/Shanghai)
**作者**：A 区（成员 A / 你）
